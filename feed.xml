<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://stephentea.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://stephentea.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-09-09T01:11:33+00:00</updated><id>https://stephentea.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Writing a Hypervisor from Scratch - Software</title><link href="https://stephentea.github.io/blog/2025/vtx-bhyve/" rel="alternate" type="text/html" title="Writing a Hypervisor from Scratch - Software"/><published>2025-08-15T00:00:00+00:00</published><updated>2025-08-15T00:00:00+00:00</updated><id>https://stephentea.github.io/blog/2025/vtx-bhyve</id><content type="html" xml:base="https://stephentea.github.io/blog/2025/vtx-bhyve/"><![CDATA[<p>This is the second post in my series exploring how to write a hypervisor from scratch. In the <a href="/blog/2025/vtx-hardware">first post of the series</a>, we motivated virtualization and took a look at the modern hardware virtualization support (specifically Intel VT-x). In this post, I will explore the design details and walk through the code of <a href="https://bhyve.org/">BHyVe</a>, which is a hypervisor for FreeBSD.</p> <p>Again, this post will largely follow <a href="https://syuu1228.github.io/howto_implement_hypervisor/">ハイパーバイザの作り方</a>, a Japanese blog series on hypervisor development, but I will supplement these notes with my own experience working on <a href="https://github.com/matsu/bitvisor">BitVisor</a>, a lightweight, para-passthrough hypervisor supporting MacOS, Windows, and Linux, running on top of both Intel and AMD CPUs.</p> <h1 id="what-is-bhyve">What is BHyVe?</h1> <p>BHyVe is a hypervisor for FreeBSD which started development in 2011. It consists of a kernel module <code class="language-plaintext highlighter-rouge">vmm.ko</code> which executes instructions closer to harder (for example, issuing VT-x instructions and modifying VMCS) and a set of userspace programs for emulating hardware, allowing users to launch/shutdown/destroy VMs, etc. This structure is similar to QEMU/KVM for Linux.</p> <p>Userspace programs mainly consist of three components: (1) <code class="language-plaintext highlighter-rouge">/usr/sbin/bhyve</code> performs CPU + hardware emulation, including emulation of virtio-pci, virtio-net, and virtio-blk (2) <code class="language-plaintext highlighter-rouge">usr/sbin/bhyveload</code> loads the OS image of a guest VM into the guest memory region, and (3) <code class="language-plaintext highlighter-rouge">/usr/sbin/bhyvectl</code> provides an interface for users to manage their VM instances. These userspace programs interface with <code class="language-plaintext highlighter-rouge">vmm.ko</code> via <code class="language-plaintext highlighter-rouge">libvmmapi</code>, which issues commands to <code class="language-plaintext highlighter-rouge">vmm.ko</code> via <code class="language-plaintext highlighter-rouge">mmap</code> and <code class="language-plaintext highlighter-rouge">ioctl</code>. A high-level system diagram is shown below.</p> <div class="row"> <div class="col-sm mt-3 mt-md-0 text-center"> <figure> <picture> <source class="responsive-img-srcset" srcset="/bhyve-imgs/bhyve-diagram-480.webp 480w,/bhyve-imgs/bhyve-diagram-800.webp 800w,/bhyve-imgs/bhyve-diagram-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/bhyve-imgs/bhyve-diagram.png" class="img-fluid rounded z-depth-1" width="300" height="400" title="diagram" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> High-level architecture diagram of BHyVe. </div> <h1 id="control-loop">Control Loop</h1> <p>Let’s first start with the main control loop of running a VM instance. To initialize a VM instance, <code class="language-plaintext highlighter-rouge">usr/sbin/bhyveload</code> creates a device file in <code class="language-plaintext highlighter-rouge">/dev/vmm/</code>, and uses this device file to issue commands to <code class="language-plaintext highlighter-rouge">vmm.ko</code> to load the guest OS image into memory, initialize registers, set GDT/IDT, etc. Note that this article assumes that BHyVe does not support BIOS/UEFI, but this has been implemented in recent versions of BHyVe. Refer to <a href="#loading--initializing-guest-vm">this section</a> for a detailed explanation of loading + initializing guest VMs.</p> <p>After <code class="language-plaintext highlighter-rouge">usr/sbin/bhyveload</code> finishes initialization, <code class="language-plaintext highlighter-rouge">usr/sbin/bhyve</code> will open this <code class="language-plaintext highlighter-rouge">/dev/vmm/</code> file, initialize device emulators, spin up one thread for each vCPU, and issue <code class="language-plaintext highlighter-rouge">ioctl(VM_RUN)</code> to <code class="language-plaintext highlighter-rouge">vmm.ko</code>. <code class="language-plaintext highlighter-rouge">vmm.ko</code> will then perform VMENTRY to switch the CPU to non-root mode. Once a VMEXIT happens, <code class="language-plaintext highlighter-rouge">vmm.ko</code> will either handle the event internally, or return to <code class="language-plaintext highlighter-rouge">usr/sbin/bhyve</code> via <code class="language-plaintext highlighter-rouge">ioctl</code> if necessary (for instance, device emulation is required).</p> <div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/bhyve-imgs/bhyve-arch-480.webp 480w,/bhyve-imgs/bhyve-arch-800.webp 800w,/bhyve-imgs/bhyve-arch-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/bhyve-imgs/bhyve-arch.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="arch" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Main control loop of BHyVe. </div> <p>Now, let’s take a look at the source code for <code class="language-plaintext highlighter-rouge">usr/sbin/bhyve</code>, <code class="language-plaintext highlighter-rouge">libvmmapi</code>, <code class="language-plaintext highlighter-rouge">vmm.ko</code>, and device emulation.</p> <h2 id="usrsbinbhyve"><code class="language-plaintext highlighter-rouge">usr/sbin/bhyve</code></h2> <p>We first look at the entry point of the file <code class="language-plaintext highlighter-rouge">usr/sbin/bhyve/bhyverun.c</code>. Note that all of these code snippets are simplified, with some details such as error handling taken out for brevity.</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span>
<span class="nf">main</span><span class="p">(</span><span class="kt">int</span> <span class="n">argc</span><span class="p">,</span> <span class="kt">char</span> <span class="o">*</span><span class="n">argv</span><span class="p">[])</span>
<span class="p">{</span>
  <span class="k">while</span> <span class="p">((</span><span class="n">c</span> <span class="o">=</span> <span class="n">getopt</span><span class="p">(</span><span class="n">argc</span><span class="p">,</span> <span class="n">argv</span><span class="p">,</span> <span class="s">"abehABHIPxp:g:c:z:s:S:n:m:M:"</span><span class="p">))</span> <span class="o">!=</span> <span class="o">-</span><span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">vmname</span> <span class="o">=</span> <span class="n">argv</span><span class="p">[</span><span class="mi">0</span><span class="p">];</span>
    <span class="n">ctx</span> <span class="o">=</span> <span class="n">vm_open</span><span class="p">(</span><span class="n">vmname</span><span class="p">);</span>  <span class="c1">// (1)</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">ctx</span> <span class="o">==</span> <span class="nb">NULL</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">perror</span><span class="p">(</span><span class="s">"vm_open"</span><span class="p">);</span>
    <span class="n">exit</span><span class="p">(</span><span class="mi">1</span><span class="p">);</span>
    <span class="p">}</span>
 
  <span class="k">if</span> <span class="p">(</span><span class="n">lomem_sz</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">lomem_addr</span> <span class="o">=</span> <span class="n">vm_map_memory</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="n">lomem_sz</span><span class="p">);</span> <span class="c1">// (2) </span>
    <span class="k">if</span> <span class="p">(</span><span class="n">lomem_addr</span> <span class="o">==</span> <span class="p">(</span><span class="kt">char</span> <span class="o">*</span><span class="p">)</span> <span class="n">MAP_FAILED</span><span class="p">)</span> <span class="p">{</span>
      <span class="n">lomem_sz</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
    <span class="p">}</span> <span class="k">else</span> <span class="k">if</span> <span class="p">(</span><span class="n">himem_sz</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
      <span class="n">himem_addr</span> <span class="o">=</span> <span class="n">vm_map_memory</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="mi">4</span><span class="o">*</span><span class="n">GB</span><span class="p">,</span> <span class="n">himem_sz</span><span class="p">);</span>  <span class="c1">// (3)</span>
      <span class="k">if</span> <span class="p">(</span><span class="n">himem_addr</span> <span class="o">==</span> <span class="p">(</span><span class="kt">char</span> <span class="o">*</span><span class="p">)</span> <span class="n">MAP_FAILED</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">lomem_sz</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
        <span class="n">himem_sz</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
      <span class="p">}</span>
    <span class="p">}</span>
  <span class="p">}</span>
  
  <span class="cm">/* (4) */</span> 
  <span class="n">init_inout</span><span class="p">();</span> 
  <span class="n">init_pci</span><span class="p">(</span><span class="n">ctx</span><span class="p">);</span> 
  <span class="k">if</span> <span class="p">(</span><span class="n">ioapic</span><span class="p">)</span>
    <span class="n">ioapic_init</span><span class="p">(</span><span class="mi">0</span><span class="p">);</span>
  
  <span class="cm">/* (5) */</span>
  <span class="n">error</span> <span class="o">=</span> <span class="n">vm_get_register</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">BSP</span><span class="p">,</span> <span class="n">VM_REG_GUEST_RIP</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">rip</span><span class="p">);</span> 
  <span class="n">assert</span><span class="p">(</span><span class="n">error</span> <span class="o">==</span> <span class="mi">0</span><span class="p">);</span>

  <span class="n">mptable_build</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">guest_ncpus</span><span class="p">,</span> <span class="n">ioapic</span><span class="p">);</span>  <span class="c1">// (6)</span>
  <span class="k">if</span> <span class="p">(</span><span class="n">acpi</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">error</span> <span class="o">=</span> <span class="n">acpi_build</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">guest_ncpus</span><span class="p">,</span> <span class="n">ioapic</span><span class="p">);</span>  <span class="c1">// (7)</span>
    <span class="n">assert</span><span class="p">(</span><span class="n">error</span> <span class="o">==</span> <span class="mi">0</span><span class="p">);</span>
  <span class="p">}</span>

  <span class="n">fbsdrun_addcpu</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">BSP</span><span class="p">,</span> <span class="n">rip</span><span class="p">);</span>   <span class="c1">// (8)</span>
  <span class="p">...</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p>In (1), the user passes in the name of the VM instance to start, and this program will open the corresponding <code class="language-plaintext highlighter-rouge">/dev/vmm</code> file. In (2) and (3), guest memory address space is mapped to low-memory and high-memory, respectively. In (4), the program initializes the emulators for I/O ports, PCI, and I/O APIC. In (5), the program obtains the value of guest RIP (instruction pointer). In (6), the program initializes the MP table (needed if more than 1 CPU). In (7), the program initializes ACPI.<br/> In (8), the program launches the thread corresponding to CPU0 (BSP), passing in the guest RIP.</p> <p>Now, let’s look at the function <code class="language-plaintext highlighter-rouge">fbsdrun_addcpu</code>:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">void</span>
<span class="nf">fbsdrun_addcpu</span><span class="p">(</span><span class="k">struct</span> <span class="n">vmctx</span> <span class="o">*</span><span class="n">ctx</span><span class="p">,</span> <span class="kt">int</span> <span class="n">vcpu</span><span class="p">,</span> <span class="kt">uint64_t</span> <span class="n">rip</span><span class="p">)</span>
<span class="p">{</span>
  <span class="kt">int</span> <span class="n">error</span><span class="p">;</span>
  <span class="k">if</span> <span class="p">(</span><span class="n">cpumask</span> <span class="o">&amp;</span> <span class="p">(</span><span class="mi">1</span> <span class="o">&lt;&lt;</span> <span class="n">vcpu</span><span class="p">))</span> <span class="p">{</span>
    <span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"addcpu: attempting to add existing cpu %d</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">vcpu</span><span class="p">);</span>
    <span class="n">exit</span><span class="p">(</span><span class="mi">1</span><span class="p">);</span>
  <span class="p">}</span>
  <span class="n">cpumask</span> <span class="o">|=</span> <span class="mi">1</span> <span class="o">&lt;&lt;</span> <span class="n">vcpu</span><span class="p">;</span>
  <span class="n">foundcpus</span><span class="o">++</span><span class="p">;</span>

  <span class="cm">/* (9) */</span>
  <span class="n">vmexit</span><span class="p">[</span><span class="n">vcpu</span><span class="p">].</span><span class="n">rip</span> <span class="o">=</span> <span class="n">rip</span><span class="p">;</span>
  <span class="n">vmexit</span><span class="p">[</span><span class="n">vcpu</span><span class="p">].</span><span class="n">inst_length</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>

  <span class="k">if</span> <span class="p">(</span><span class="n">vcpu</span> <span class="o">==</span> <span class="n">BSP</span> <span class="o">||</span> <span class="o">!</span><span class="n">guest_vcpu_mux</span><span class="p">){</span>
    <span class="n">mt_vmm_info</span><span class="p">[</span><span class="n">vcpu</span><span class="p">].</span><span class="n">mt_ctx</span> <span class="o">=</span> <span class="n">ctx</span><span class="p">;</span>
    <span class="n">mt_vmm_info</span><span class="p">[</span><span class="n">vcpu</span><span class="p">].</span><span class="n">mt_vcpu</span> <span class="o">=</span> <span class="n">vcpu</span><span class="p">;</span>
    <span class="n">error</span> <span class="o">=</span> <span class="n">pthread_create</span><span class="p">(</span><span class="o">&amp;</span><span class="n">mt_vmm_info</span><span class="p">[</span><span class="n">vcpu</span><span class="p">].</span><span class="n">mt_thr</span><span class="p">,</span> <span class="nb">NULL</span><span class="p">,</span>  <span class="c1">// (10)</span>
                           <span class="n">fbsdrun_start_thread</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">mt_vmm_info</span><span class="p">[</span><span class="n">vcpu</span><span class="p">]);</span> 
    <span class="n">assert</span><span class="p">(</span><span class="n">error</span> <span class="o">==</span> <span class="mi">0</span><span class="p">);</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p>In (9), we set up the <code class="language-plaintext highlighter-rouge">vmexit</code> struct to allow the guest to resume execution at the desired location upon a VMEXIT. In (10), we spin up a thread to begin emulation of the vCPU. Let’s look at the function <code class="language-plaintext highlighter-rouge">fbsdrun_start_thread</code>:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">static</span> <span class="kt">void</span> <span class="o">*</span>
<span class="nf">fbsdrun_start_thread</span><span class="p">(</span><span class="kt">void</span> <span class="o">*</span><span class="n">param</span><span class="p">)</span>
<span class="p">{</span>
  <span class="kt">char</span> <span class="n">tname</span><span class="p">[</span><span class="n">MAXCOMLEN</span> <span class="o">+</span> <span class="mi">1</span><span class="p">];</span>
  <span class="k">struct</span> <span class="n">mt_vmm_info</span> <span class="o">*</span><span class="n">mtp</span><span class="p">;</span>
  <span class="kt">int</span> <span class="n">vcpu</span><span class="p">;</span>
  <span class="n">mtp</span> <span class="o">=</span> <span class="n">param</span><span class="p">;</span>
  <span class="n">vcpu</span> <span class="o">=</span> <span class="n">mtp</span><span class="o">-&gt;</span><span class="n">mt_vcpu</span><span class="p">;</span>
  <span class="n">snprintf</span><span class="p">(</span><span class="n">tname</span><span class="p">,</span> <span class="k">sizeof</span><span class="p">(</span><span class="n">tname</span><span class="p">),</span> <span class="s">"%s vcpu %d"</span><span class="p">,</span> <span class="n">vmname</span><span class="p">,</span> <span class="n">vcpu</span><span class="p">);</span>
  <span class="n">pthread_set_name_np</span><span class="p">(</span><span class="n">mtp</span><span class="o">-&gt;</span><span class="n">mt_thr</span><span class="p">,</span> <span class="n">tname</span><span class="p">);</span>
  <span class="n">vm_loop</span><span class="p">(</span><span class="n">mtp</span><span class="o">-&gt;</span><span class="n">mt_ctx</span><span class="p">,</span> <span class="n">vcpu</span><span class="p">,</span> <span class="n">vmexit</span><span class="p">[</span><span class="n">vcpu</span><span class="p">].</span><span class="n">rip</span><span class="p">);</span>  <span class="c1">// (11)</span>
  
  <span class="cm">/* will not reach here! */</span>
  <span class="n">exit</span><span class="p">(</span><span class="mi">1</span><span class="p">);</span>
  <span class="k">return</span> <span class="p">(</span><span class="nb">NULL</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <p>This function sets the name for the vCPU and begins the main control loop <code class="language-plaintext highlighter-rouge">vm_loop</code> in (11). Note that <code class="language-plaintext highlighter-rouge">vm_loop</code> is a <code class="language-plaintext highlighter-rouge">while (1)</code> loop, hence this function will never return. Let’s take a look at <code class="language-plaintext highlighter-rouge">vm_loop</code>:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">static</span> <span class="n">vmexit_handler_t</span> <span class="n">handler</span><span class="p">[</span><span class="n">VM_EXITCODE_MAX</span><span class="p">]</span> <span class="o">=</span> <span class="p">{</span>
  <span class="p">[</span><span class="n">VM_EXITCODE_INOUT</span><span class="p">]</span> <span class="o">=</span> <span class="n">vmexit_inout</span><span class="p">,</span>
  <span class="p">[</span><span class="n">VM_EXITCODE_VMX</span><span class="p">]</span> <span class="o">=</span> <span class="n">vmexit_vmx</span><span class="p">,</span>
  <span class="p">[</span><span class="n">VM_EXITCODE_BOGUS</span><span class="p">]</span> <span class="o">=</span> <span class="n">vmexit_bogus</span><span class="p">,</span>
  <span class="p">[</span><span class="n">VM_EXITCODE_RDMSR</span><span class="p">]</span> <span class="o">=</span> <span class="n">vmexit_rdmsr</span><span class="p">,</span>
  <span class="p">[</span><span class="n">VM_EXITCODE_WRMSR</span><span class="p">]</span> <span class="o">=</span> <span class="n">vmexit_wrmsr</span><span class="p">,</span>
  <span class="p">[</span><span class="n">VM_EXITCODE_MTRAP</span><span class="p">]</span> <span class="o">=</span> <span class="n">vmexit_mtrap</span><span class="p">,</span>
  <span class="p">[</span><span class="n">VM_EXITCODE_PAGING</span><span class="p">]</span> <span class="o">=</span> <span class="n">vmexit_paging</span><span class="p">,</span>
  <span class="p">[</span><span class="n">VM_EXITCODE_SPINUP_AP</span><span class="p">]</span> <span class="o">=</span> <span class="n">vmexit_spinup_ap</span><span class="p">,</span>
<span class="p">};</span>

<span class="k">static</span> <span class="kt">void</span>
<span class="nf">vm_loop</span><span class="p">(</span><span class="k">struct</span> <span class="n">vmctx</span> <span class="o">*</span><span class="n">ctx</span><span class="p">,</span> <span class="kt">int</span> <span class="n">vcpu</span><span class="p">,</span> <span class="kt">uint64_t</span> <span class="n">rip</span><span class="p">)</span>
<span class="p">{</span>
  <span class="kt">int</span> <span class="n">error</span><span class="p">,</span> <span class="n">rc</span><span class="p">,</span> <span class="n">prevcpu</span><span class="p">;</span>
  <span class="k">if</span> <span class="p">(</span><span class="n">guest_vcpu_mux</span><span class="p">)</span>
    <span class="n">setup_timeslice</span><span class="p">();</span>
  <span class="k">if</span> <span class="p">(</span><span class="n">pincpu</span> <span class="o">&gt;=</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">error</span> <span class="o">=</span> <span class="n">vm_set_pinning</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">vcpu</span><span class="p">,</span> <span class="n">pincpu</span> <span class="o">+</span> <span class="n">vcpu</span><span class="p">);</span>
    <span class="n">assert</span><span class="p">(</span><span class="n">error</span> <span class="o">==</span> <span class="mi">0</span><span class="p">);</span>
  <span class="p">}</span>
  <span class="k">while</span> <span class="p">(</span><span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">error</span> <span class="o">=</span> <span class="n">vm_run</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">vcpu</span><span class="p">,</span> <span class="n">rip</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">vmexit</span><span class="p">[</span><span class="n">vcpu</span><span class="p">]);</span>  <span class="c1">// (12)</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">error</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
      <span class="cm">/*
      * It is possible that 'vmmctl' or some other process
      * has transitioned the vcpu to CANNOT_RUN state right
      * before we tried to transition it to RUNNING.
      * This is expected to be temporary so just retry.
      */</span>
      <span class="k">if</span> <span class="p">(</span><span class="n">errno</span> <span class="o">==</span> <span class="n">EBUSY</span><span class="p">)</span>
        <span class="k">continue</span><span class="p">;</span>
      <span class="k">else</span>
        <span class="k">break</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="n">prevcpu</span> <span class="o">=</span> <span class="n">vcpu</span><span class="p">;</span>
    <span class="n">rc</span> <span class="o">=</span> <span class="p">(</span><span class="o">*</span><span class="n">handler</span><span class="p">[</span><span class="n">vmexit</span><span class="p">[</span><span class="n">vcpu</span><span class="p">].</span><span class="n">exitcode</span><span class="p">])(</span><span class="n">ctx</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">vmexit</span><span class="p">[</span><span class="n">vcpu</span><span class="p">],</span> <span class="o">&amp;</span><span class="n">vcpu</span><span class="p">);</span>  <span class="c1">// (13)</span>
    <span class="k">switch</span> <span class="p">(</span><span class="n">rc</span><span class="p">)</span> <span class="p">{</span> <span class="p">(</span><span class="mi">23</span><span class="p">)</span>
      <span class="k">case</span> <span class="n">VMEXIT_SWITCH</span><span class="p">:</span>  <span class="c1">// (14) </span>
        <span class="n">assert</span><span class="p">(</span><span class="n">guest_vcpu_mux</span><span class="p">);</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">vcpu</span> <span class="o">==</span> <span class="o">-</span><span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
          <span class="n">stats</span><span class="p">.</span><span class="n">cpu_switch_rotate</span><span class="o">++</span><span class="p">;</span>
          <span class="n">vcpu</span> <span class="o">=</span> <span class="n">fbsdrun_get_next_cpu</span><span class="p">(</span><span class="n">prevcpu</span><span class="p">);</span>
        <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
          <span class="n">stats</span><span class="p">.</span><span class="n">cpu_switch_direct</span><span class="o">++</span><span class="p">;</span>
        <span class="p">}</span>
      <span class="k">case</span> <span class="n">VMEXIT_CONTINUE</span><span class="p">:</span>  <span class="c1">// (15)</span>
        <span class="n">rip</span> <span class="o">=</span> <span class="n">vmexit</span><span class="p">[</span><span class="n">vcpu</span><span class="p">].</span><span class="n">rip</span> <span class="o">+</span> <span class="n">vmexit</span><span class="p">[</span><span class="n">vcpu</span><span class="p">].</span><span class="n">inst_length</span><span class="p">;</span>
        <span class="k">break</span><span class="p">;</span>
      <span class="k">case</span> <span class="n">VMEXIT_RESTART</span><span class="p">:</span>   <span class="c1">// (16)</span>
        <span class="n">rip</span> <span class="o">=</span> <span class="n">vmexit</span><span class="p">[</span><span class="n">vcpu</span><span class="p">].</span><span class="n">rip</span><span class="p">;</span>
        <span class="k">break</span><span class="p">;</span>
      <span class="k">case</span> <span class="n">VMEXIT_RESET</span><span class="p">:</span>     <span class="c1">// (17)</span>
        <span class="n">exit</span><span class="p">(</span><span class="mi">0</span><span class="p">);</span>
      <span class="nl">default:</span>
        <span class="n">exit</span><span class="p">(</span><span class="mi">1</span><span class="p">);</span>
    <span class="p">}</span>
  <span class="p">}</span>
  <span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"vm_run error %d, errno %d</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">error</span><span class="p">,</span> <span class="n">errno</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <p>In (12), <code class="language-plaintext highlighter-rouge">/usr/sbin/bhyve</code> calls <code class="language-plaintext highlighter-rouge">vm_run</code>, which is an API provided by <code class="language-plaintext highlighter-rouge">libvmmapi</code> library to issue <code class="language-plaintext highlighter-rouge">ioctl</code> to <code class="language-plaintext highlighter-rouge">vmm.ko</code>. Once <code class="language-plaintext highlighter-rouge">vmm.ko</code> returns (for instance, device emulation is needed), this program invokes the correct handler based on <code class="language-plaintext highlighter-rouge">exitcode</code> field of <code class="language-plaintext highlighter-rouge">vmexit</code> struct. The program then either switches to a different vCPU (14), continues execution of the next guest instruction (15), reruns the current guest instruction (16), or exits vCPU emulation (17).</p> <h2 id="libvmmapi"><code class="language-plaintext highlighter-rouge">libvmmapi</code></h2> <p>Let’s take a look at the different API functions that <code class="language-plaintext highlighter-rouge">libvmmapi</code> provides to userspace programs for interfacing with <code class="language-plaintext highlighter-rouge">vmm.ko</code>.</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">static</span> <span class="kt">int</span>
<span class="nf">vm_device_open</span><span class="p">(</span><span class="k">const</span> <span class="kt">char</span> <span class="o">*</span><span class="n">name</span><span class="p">)</span>
<span class="p">{</span>
  <span class="kt">int</span> <span class="n">fd</span><span class="p">,</span> <span class="n">len</span><span class="p">;</span>
  <span class="kt">char</span> <span class="o">*</span><span class="n">vmfile</span><span class="p">;</span>
  <span class="n">len</span> <span class="o">=</span> <span class="n">strlen</span><span class="p">(</span><span class="s">"/dev/vmm/"</span><span class="p">)</span> <span class="o">+</span> <span class="n">strlen</span><span class="p">(</span><span class="n">name</span><span class="p">)</span> <span class="o">+</span> <span class="mi">1</span><span class="p">;</span>
  <span class="n">vmfile</span> <span class="o">=</span> <span class="n">malloc</span><span class="p">(</span><span class="n">len</span><span class="p">);</span>
  <span class="n">assert</span><span class="p">(</span><span class="n">vmfile</span> <span class="o">!=</span> <span class="nb">NULL</span><span class="p">);</span>
  <span class="n">snprintf</span><span class="p">(</span><span class="n">vmfile</span><span class="p">,</span> <span class="n">len</span><span class="p">,</span> <span class="s">"/dev/vmm/%s"</span><span class="p">,</span> <span class="n">name</span><span class="p">);</span>
  
  <span class="n">fd</span> <span class="o">=</span> <span class="n">open</span><span class="p">(</span><span class="n">vmfile</span><span class="p">,</span> <span class="n">O_RDWR</span><span class="p">,</span> <span class="mi">0</span><span class="p">);</span>  <span class="c1">// (3)</span>
  <span class="n">free</span><span class="p">(</span><span class="n">vmfile</span><span class="p">);</span>
  <span class="k">return</span> <span class="p">(</span><span class="n">fd</span><span class="p">);</span>
<span class="p">}</span>

<span class="n">vm_open</span><span class="p">(</span><span class="k">const</span> <span class="kt">char</span> <span class="o">*</span><span class="n">name</span><span class="p">)</span>
<span class="p">{</span>
  <span class="k">struct</span> <span class="n">vmctx</span> <span class="o">*</span><span class="n">vm</span><span class="p">;</span>
  <span class="n">vm</span> <span class="o">=</span> <span class="n">malloc</span><span class="p">(</span><span class="k">sizeof</span><span class="p">(</span><span class="k">struct</span> <span class="n">vmctx</span><span class="p">)</span> <span class="o">+</span> <span class="n">strlen</span><span class="p">(</span><span class="n">name</span><span class="p">)</span> <span class="o">+</span> <span class="mi">1</span><span class="p">);</span>  <span class="c1">// (1)</span>
  <span class="n">assert</span><span class="p">(</span><span class="n">vm</span> <span class="o">!=</span> <span class="nb">NULL</span><span class="p">);</span>
  <span class="n">vm</span><span class="o">-&gt;</span><span class="n">fd</span> <span class="o">=</span> <span class="o">-</span><span class="mi">1</span><span class="p">;</span>
  <span class="n">vm</span><span class="o">-&gt;</span><span class="n">name</span> <span class="o">=</span> <span class="p">(</span><span class="kt">char</span> <span class="o">*</span><span class="p">)(</span><span class="n">vm</span> <span class="o">+</span> <span class="mi">1</span><span class="p">);</span>
  <span class="n">strcpy</span><span class="p">(</span><span class="n">vm</span><span class="o">-&gt;</span><span class="n">name</span><span class="p">,</span> <span class="n">name</span><span class="p">);</span>
  <span class="k">if</span> <span class="p">((</span><span class="n">vm</span><span class="o">-&gt;</span><span class="n">fd</span> <span class="o">=</span> <span class="n">vm_device_open</span><span class="p">(</span><span class="n">vm</span><span class="o">-&gt;</span><span class="n">name</span><span class="p">))</span> <span class="o">&lt;</span> <span class="mi">0</span><span class="p">)</span>  <span class="c1">// (2)</span>
    <span class="k">goto</span> <span class="n">err</span><span class="p">;</span>
  <span class="k">return</span> <span class="p">(</span><span class="n">vm</span><span class="p">);</span>

<span class="nl">err:</span>
  <span class="n">vm_destroy</span><span class="p">(</span><span class="n">vm</span><span class="p">);</span>
  <span class="k">return</span> <span class="p">(</span><span class="nb">NULL</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <p>In (1) of the code of the <a href="#usrsbinbhyve">previous section</a>, we saw the function <code class="language-plaintext highlighter-rouge">vm_open</code>, which is implemented here. This function allocates a <code class="language-plaintext highlighter-rouge">vmctx</code> struct in (1), and opens the file descriptor in <code class="language-plaintext highlighter-rouge">/dev/vmm</code> by calling <code class="language-plaintext highlighter-rouge">vm_device_open</code> in (2). <code class="language-plaintext highlighter-rouge">vm_device_open</code> uses the underlying <code class="language-plaintext highlighter-rouge">open</code> syscall in (3).</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">char</span> <span class="o">*</span>
<span class="nf">vm_map_memory</span><span class="p">(</span><span class="k">struct</span> <span class="n">vmctx</span> <span class="o">*</span><span class="n">ctx</span><span class="p">,</span> <span class="n">vm_paddr_t</span> <span class="n">gpa</span><span class="p">,</span> <span class="kt">size_t</span> <span class="n">len</span><span class="p">)</span>
<span class="p">{</span>
  <span class="k">return</span> <span class="p">((</span><span class="kt">char</span> <span class="o">*</span><span class="p">)</span><span class="n">mmap</span><span class="p">(</span><span class="nb">NULL</span><span class="p">,</span> <span class="n">len</span><span class="p">,</span> <span class="n">PROT_READ</span> <span class="o">|</span> <span class="n">PROT_WRITE</span><span class="p">,</span> <span class="n">MAP_SHARED</span><span class="p">,</span>
                       <span class="n">ctx</span><span class="o">-&gt;</span><span class="n">fd</span><span class="p">,</span> <span class="n">gpa</span><span class="p">));</span>
<span class="p">}</span>
</code></pre></div></div> <p>In (2) and (3) of the code of the <a href="#usrsbinbhyve">previous section</a>, we saw the function <code class="language-plaintext highlighter-rouge">vm_map_memory</code>, which is implemented here. This function maps the VM device file <code class="language-plaintext highlighter-rouge">/dev/vmm</code> into guest physical address <code class="language-plaintext highlighter-rouge">gpa</code> using the <code class="language-plaintext highlighter-rouge">mmap</code> syscall.</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span>
<span class="nf">vm_get_register</span><span class="p">(</span><span class="k">struct</span> <span class="n">vmctx</span> <span class="o">*</span><span class="n">ctx</span><span class="p">,</span> <span class="kt">int</span> <span class="n">vcpu</span><span class="p">,</span> <span class="kt">int</span> <span class="n">reg</span><span class="p">,</span> <span class="kt">uint64_t</span> <span class="o">*</span><span class="n">ret_val</span><span class="p">)</span>
<span class="p">{</span>
  <span class="kt">int</span> <span class="n">error</span><span class="p">;</span>
  <span class="k">struct</span> <span class="n">vm_register</span> <span class="n">vmreg</span><span class="p">;</span>
  <span class="n">bzero</span><span class="p">(</span><span class="o">&amp;</span><span class="n">vmreg</span><span class="p">,</span> <span class="k">sizeof</span><span class="p">(</span><span class="n">vmreg</span><span class="p">));</span>
  <span class="n">vmreg</span><span class="p">.</span><span class="n">cpuid</span> <span class="o">=</span> <span class="n">vcpu</span><span class="p">;</span>
  <span class="n">vmreg</span><span class="p">.</span><span class="n">regnum</span> <span class="o">=</span> <span class="n">reg</span><span class="p">;</span>
  <span class="n">error</span> <span class="o">=</span> <span class="n">ioctl</span><span class="p">(</span><span class="n">ctx</span><span class="o">-&gt;</span><span class="n">fd</span><span class="p">,</span> <span class="n">VM_GET_REGISTER</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">vmreg</span><span class="p">);</span>  <span class="c1">// (3)</span>
  <span class="o">*</span><span class="n">ret_val</span> <span class="o">=</span> <span class="n">vmreg</span><span class="p">.</span><span class="n">regval</span><span class="p">;</span>
  <span class="k">return</span> <span class="p">(</span><span class="n">error</span><span class="p">);</span>
  <span class="p">}</span>
</code></pre></div></div> <p>In (5) of the code of the <a href="#usrsbinbhyve">previous section</a>, we saw the function <code class="language-plaintext highlighter-rouge">vm_get_register</code>, which is implemented here. This function is simply a wrapper around <code class="language-plaintext highlighter-rouge">ioctl(VM_GET_REGISTER)</code>, which is a command to <code class="language-plaintext highlighter-rouge">vmm.ko</code>. This function then takes the return value from <code class="language-plaintext highlighter-rouge">vmm.ko</code> and sets the <code class="language-plaintext highlighter-rouge">regval</code> field in <code class="language-plaintext highlighter-rouge">struct vm_register</code>.</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span>
<span class="nf">vm_run</span><span class="p">(</span><span class="k">struct</span> <span class="n">vmctx</span> <span class="o">*</span><span class="n">ctx</span><span class="p">,</span> <span class="kt">int</span> <span class="n">vcpu</span><span class="p">,</span> <span class="kt">uint64_t</span> <span class="n">rip</span><span class="p">,</span> <span class="k">struct</span> <span class="n">vm_exit</span> <span class="o">*</span><span class="n">vmexit</span><span class="p">)</span>
<span class="p">{</span>
  <span class="kt">int</span> <span class="n">error</span><span class="p">;</span>
  <span class="k">struct</span> <span class="n">vm_run</span> <span class="n">vmrun</span><span class="p">;</span>
  <span class="n">bzero</span><span class="p">(</span><span class="o">&amp;</span><span class="n">vmrun</span><span class="p">,</span> <span class="k">sizeof</span><span class="p">(</span><span class="n">vmrun</span><span class="p">));</span>
  <span class="n">vmrun</span><span class="p">.</span><span class="n">cpuid</span> <span class="o">=</span> <span class="n">vcpu</span><span class="p">;</span>
  <span class="n">vmrun</span><span class="p">.</span><span class="n">rip</span> <span class="o">=</span> <span class="n">rip</span><span class="p">;</span>
  <span class="n">error</span> <span class="o">=</span> <span class="n">ioctl</span><span class="p">(</span><span class="n">ctx</span><span class="o">-&gt;</span><span class="n">fd</span><span class="p">,</span> <span class="n">VM_RUN</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">vmrun</span><span class="p">);</span> <span class="p">(</span><span class="mi">19</span><span class="p">)</span>
  <span class="n">bcopy</span><span class="p">(</span><span class="o">&amp;</span><span class="n">vmrun</span><span class="p">.</span><span class="n">vm_exit</span><span class="p">,</span> <span class="n">vmexit</span><span class="p">,</span> <span class="k">sizeof</span><span class="p">(</span><span class="k">struct</span> <span class="n">vm_exit</span><span class="p">));</span>  <span class="c1">// (4)</span>
  <span class="k">return</span> <span class="p">(</span><span class="n">error</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <p>In (12) of the code of the <a href="#usrsbinbhyve">previous section</a>, we saw the function <code class="language-plaintext highlighter-rouge">vm_run</code>, which is implemented here. This function is simply a wrapper around <code class="language-plaintext highlighter-rouge">ioctl(VM_RUN)</code>, which starts guest execution at <code class="language-plaintext highlighter-rouge">vmmrun.rip</code>. If a VMEXIT occurs that <code class="language-plaintext highlighter-rouge">vmm.ko</code> cannot handle by itself, <code class="language-plaintext highlighter-rouge">ioctl</code> will return with an error code. This function stores the exit information in <code class="language-plaintext highlighter-rouge">vmrun.vm_exit</code> in (4).</p> <h2 id="vmmko"><code class="language-plaintext highlighter-rouge">vmm.ko</code></h2> <p>We now look at the implementation of the kernel module <code class="language-plaintext highlighter-rouge">vmm.ko</code>.</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">static</span> <span class="kt">int</span>
<span class="nf">vmmdev_ioctl</span><span class="p">(</span><span class="k">struct</span> <span class="n">cdev</span> <span class="o">*</span><span class="n">cdev</span><span class="p">,</span> <span class="n">u_long</span> <span class="n">cmd</span><span class="p">,</span> <span class="n">caddr_t</span> <span class="n">data</span><span class="p">,</span> <span class="kt">int</span> <span class="n">fflag</span><span class="p">,</span>
             <span class="k">struct</span> <span class="kr">thread</span> <span class="o">*</span><span class="n">td</span><span class="p">)</span>
<span class="p">{</span>
<span class="k">switch</span><span class="p">(</span><span class="n">cmd</span><span class="p">)</span> <span class="p">{</span>
  <span class="k">case</span> <span class="n">VM_RUN</span><span class="p">:</span>
    <span class="n">vmrun</span> <span class="o">=</span> <span class="p">(</span><span class="k">struct</span> <span class="n">vm_run</span> <span class="o">*</span><span class="p">)</span><span class="n">data</span><span class="p">;</span>
    <span class="n">error</span> <span class="o">=</span> <span class="n">vm_run</span><span class="p">(</span><span class="n">sc</span><span class="o">-&gt;</span><span class="n">vm</span><span class="p">,</span> <span class="n">vmrun</span><span class="p">);</span> 
    <span class="k">break</span><span class="p">;</span>
  <span class="p">...</span>
<span class="p">}</span>
  <span class="p">...</span>
<span class="p">}</span>

<span class="k">static</span> <span class="kt">int</span>
<span class="nf">sysctl_vmm_create</span><span class="p">(</span><span class="n">SYSCTL_HANDLER_ARGS</span><span class="p">)</span>
<span class="p">{</span>
  <span class="p">...</span>
  <span class="n">vm</span> <span class="o">=</span> <span class="n">vm_create</span><span class="p">(</span><span class="n">buf</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">vmm_dev.c</code> defines the handlers for various operations on the VM device file in <code class="language-plaintext highlighter-rouge">/dev/vmm</code>, including <code class="language-plaintext highlighter-rouge">open()</code>, <code class="language-plaintext highlighter-rouge">close()</code>, <code class="language-plaintext highlighter-rouge">read()</code>, <code class="language-plaintext highlighter-rouge">write()</code>, <code class="language-plaintext highlighter-rouge">mmap()</code>, <code class="language-plaintext highlighter-rouge">ioctl()</code>. The <code class="language-plaintext highlighter-rouge">ioctl()</code> handler is shown above, which switches on <code class="language-plaintext highlighter-rouge">cmd</code> and invokes the desired function, in this case <code class="language-plaintext highlighter-rouge">vm_run</code> in response to <code class="language-plaintext highlighter-rouge">VM_RUN</code>. The function below is used to create a new VM instance using <code class="language-plaintext highlighter-rouge">vm_create</code>.</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">static</span> <span class="k">struct</span> <span class="n">vmm_ops</span> <span class="o">*</span><span class="n">ops</span><span class="p">;</span>
<span class="cp">#define VMINIT(vm) (ops != NULL ? (*ops-&gt;vminit)(vm): NULL)
#define VMRUN(vmi, vcpu, rip) (ops != NULL ? (*ops-&gt;vmrun)(vmi, vcpu, rip) : ENXIO)
</span>
<span class="k">static</span> <span class="kt">int</span>
<span class="nf">vmm_init</span><span class="p">(</span><span class="kt">void</span><span class="p">)</span>
<span class="p">{</span>
  <span class="k">if</span> <span class="p">(</span><span class="n">vmm_is_intel</span><span class="p">())</span>
    <span class="n">ops</span> <span class="o">=</span> <span class="o">&amp;</span><span class="n">vmm_ops_intel</span><span class="p">;</span>
  <span class="k">else</span> <span class="k">if</span> <span class="p">(</span><span class="n">vmm_is_amd</span><span class="p">())</span>
    <span class="n">ops</span> <span class="o">=</span> <span class="o">&amp;</span><span class="n">vmm_ops_amd</span><span class="p">;</span>
  <span class="k">return</span> <span class="p">(</span><span class="n">VMM_INIT</span><span class="p">());</span>
<span class="p">}</span>

<span class="k">struct</span> <span class="n">vm</span> <span class="o">*</span>
<span class="nf">vm_create</span><span class="p">(</span><span class="k">const</span> <span class="kt">char</span> <span class="o">*</span><span class="n">name</span><span class="p">)</span>
<span class="p">{</span>
  <span class="n">vm</span><span class="o">-&gt;</span><span class="n">cookie</span> <span class="o">=</span> <span class="n">VMINIT</span><span class="p">(</span><span class="n">vm</span><span class="p">);</span>
<span class="p">}</span>

<span class="kt">int</span>
<span class="nf">vm_run</span><span class="p">(</span><span class="k">struct</span> <span class="n">vm</span> <span class="o">*</span><span class="n">vm</span><span class="p">,</span> <span class="k">struct</span> <span class="n">vm_run</span> <span class="o">*</span><span class="n">vmrun</span><span class="p">)</span>
<span class="p">{</span>
  <span class="n">vcpuid</span> <span class="o">=</span> <span class="n">vmrun</span><span class="o">-&gt;</span><span class="n">cpuid</span><span class="p">;</span>
  <span class="n">rip</span> <span class="o">=</span> <span class="n">vmrun</span><span class="o">-&gt;</span><span class="n">rip</span><span class="p">;</span>
  <span class="n">error</span> <span class="o">=</span> <span class="n">VMRUN</span><span class="p">(</span><span class="n">vm</span><span class="o">-&gt;</span><span class="n">cookie</span><span class="p">,</span> <span class="n">vcpuid</span><span class="p">,</span> <span class="n">rip</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">vm_create</code> is a wrapper around the macro <code class="language-plaintext highlighter-rouge">VMINIT</code>, which invokes the architecture- dependent function stored in <code class="language-plaintext highlighter-rouge">struct vm_ops</code> (for example, there are separate initialization functions for VMX and SVM). Similarly, <code class="language-plaintext highlighter-rouge">vm_run</code> is a wrapper around the macro <code class="language-plaintext highlighter-rouge">VMRUN</code>. Note that <code class="language-plaintext highlighter-rouge">vmm_init</code> chooses the correct <code class="language-plaintext highlighter-rouge">struct vm_ops</code> based on the hardware architecture.</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">struct</span> <span class="n">vmm_ops</span> <span class="n">vmm_ops_intel</span> <span class="o">=</span> <span class="p">{</span>  <span class="c1">// (1)</span>
  <span class="n">vmx_init</span><span class="p">,</span>
  <span class="n">vmx_cleanup</span><span class="p">,</span>
  <span class="n">vmx_vminit</span><span class="p">,</span> 
  <span class="n">vmx_run</span><span class="p">,</span>
  <span class="n">vmx_vmcleanup</span><span class="p">,</span>
  <span class="n">ept_vmmmap_set</span><span class="p">,</span>
  <span class="n">ept_vmmmap_get</span><span class="p">,</span>
  <span class="n">vmx_getreg</span><span class="p">,</span>
  <span class="n">vmx_setreg</span><span class="p">,</span>
  <span class="n">vmx_getdesc</span><span class="p">,</span>
  <span class="n">vmx_setdesc</span><span class="p">,</span>
  <span class="n">vmx_inject</span><span class="p">,</span>
  <span class="n">vmx_getcap</span><span class="p">,</span>
  <span class="n">vmx_setcap</span>
<span class="p">};</span>

<span class="k">static</span> <span class="kt">void</span> <span class="o">*</span>
<span class="nf">vmx_vminit</span><span class="p">(</span><span class="k">struct</span> <span class="n">vm</span> <span class="o">*</span><span class="n">vm</span><span class="p">)</span>
<span class="p">{</span>
<span class="k">for</span> <span class="p">(</span><span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">VM_MAXCPU</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span> 
  <span class="cm">/* (2) */</span>
  <span class="n">error</span> <span class="o">=</span> <span class="n">vmcs_set_defaults</span><span class="p">(</span><span class="o">&amp;</span><span class="n">vmx</span><span class="o">-&gt;</span><span class="n">vmcs</span><span class="p">[</span><span class="n">i</span><span class="p">],</span>
                            <span class="p">(</span><span class="n">u_long</span><span class="p">)</span><span class="n">vmx_longjmp</span><span class="p">,</span>
                            <span class="p">(</span><span class="n">u_long</span><span class="p">)</span><span class="o">&amp;</span><span class="n">vmx</span><span class="o">-&gt;</span><span class="n">ctx</span><span class="p">[</span><span class="n">i</span><span class="p">],</span>
                            <span class="n">vtophys</span><span class="p">(</span><span class="n">vmx</span><span class="o">-&gt;</span><span class="n">pml4ept</span><span class="p">),</span>
                            <span class="n">pinbased_ctls</span><span class="p">,</span>
                            <span class="n">procbased_ctls</span><span class="p">,</span>
                            <span class="n">procbased_ctls2</span><span class="p">,</span>
                            <span class="n">exit_ctls</span><span class="p">,</span> <span class="n">entry_ctls</span><span class="p">,</span>
                            <span class="n">vtophys</span><span class="p">(</span><span class="n">vmx</span><span class="o">-&gt;</span><span class="n">msr_bitmap</span><span class="p">),</span>
                            <span class="n">vpid</span><span class="p">);</span>
  <span class="p">}</span>
  <span class="k">return</span> <span class="p">(</span><span class="n">vmx</span><span class="p">);</span>
<span class="p">}</span>

<span class="k">static</span> <span class="kt">int</span>
<span class="nf">vmx_run</span><span class="p">(</span><span class="kt">void</span> <span class="o">*</span><span class="n">arg</span><span class="p">,</span> <span class="kt">int</span> <span class="n">vcpu</span><span class="p">,</span> <span class="n">register_t</span> <span class="n">rip</span><span class="p">)</span>
<span class="p">{</span>
  <span class="n">vmxctx</span> <span class="o">=</span> <span class="o">&amp;</span><span class="n">vmx</span><span class="o">-&gt;</span><span class="n">ctx</span><span class="p">[</span><span class="n">vcpu</span><span class="p">];</span>
  <span class="cm">/* (3) */</span>
  <span class="n">VMPTRLD</span><span class="p">(</span><span class="n">vmcs</span><span class="p">);</span>
  <span class="k">if</span> <span class="p">((</span><span class="n">error</span> <span class="o">=</span> <span class="n">vmwrite</span><span class="p">(</span><span class="n">VMCS_HOST_CR3</span><span class="p">,</span> <span class="n">rcr3</span><span class="p">()))</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">)</span>
    <span class="n">panic</span><span class="p">(</span><span class="s">"vmx_run: error %d writing to VMCS_HOST_CR3"</span><span class="p">,</span> <span class="n">error</span><span class="p">);</span>
  <span class="k">if</span> <span class="p">((</span><span class="n">error</span> <span class="o">=</span> <span class="n">vmwrite</span><span class="p">(</span><span class="n">VMCS_GUEST_RIP</span><span class="p">,</span> <span class="n">rip</span><span class="p">))</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">)</span> <span class="p">(</span><span class="mi">8</span><span class="p">)</span>
    <span class="n">panic</span><span class="p">(</span><span class="s">"vmx_run: error %d writing to VMCS_GUEST_RIP"</span><span class="p">,</span> <span class="n">error</span><span class="p">);</span>
  <span class="k">if</span> <span class="p">((</span><span class="n">error</span> <span class="o">=</span> <span class="n">vmx_set_pcpu_defaults</span><span class="p">(</span><span class="n">vmx</span><span class="p">,</span> <span class="n">vcpu</span><span class="p">))</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">)</span>
    <span class="n">panic</span><span class="p">(</span><span class="s">"vmx_run: error %d setting up pcpu defaults"</span><span class="p">,</span> <span class="n">error</span><span class="p">);</span>

  <span class="k">do</span> <span class="p">{</span>  <span class="c1">// (4) </span>
    <span class="n">lapic_timer_tick</span><span class="p">(</span><span class="n">vmx</span><span class="o">-&gt;</span><span class="n">vm</span><span class="p">,</span> <span class="n">vcpu</span><span class="p">);</span>
    <span class="n">vmx_inject_interrupts</span><span class="p">(</span><span class="n">vmx</span><span class="p">,</span> <span class="n">vcpu</span><span class="p">);</span>
    <span class="n">vmx_run_trace</span><span class="p">(</span><span class="n">vmx</span><span class="p">,</span> <span class="n">vcpu</span><span class="p">);</span>
    <span class="n">rc</span> <span class="o">=</span> <span class="n">vmx_setjmp</span><span class="p">(</span><span class="n">vmxctx</span><span class="p">);</span>  <span class="c1">// (5)</span>
    <span class="k">switch</span> <span class="p">(</span><span class="n">rc</span><span class="p">)</span> <span class="p">{</span>
      <span class="k">case</span> <span class="n">VMX_RETURN_DIRECT</span><span class="p">:</span> 
        <span class="k">if</span> <span class="p">(</span><span class="n">vmxctx</span><span class="o">-&gt;</span><span class="n">launched</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span> 
          <span class="n">vmxctx</span><span class="o">-&gt;</span><span class="n">launched</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
          <span class="n">vmx_launch</span><span class="p">(</span><span class="n">vmxctx</span><span class="p">);</span>   <span class="c1">// (6)</span>
        <span class="p">}</span> <span class="k">else</span>
          <span class="n">vmx_resume</span><span class="p">(</span><span class="n">vmxctx</span><span class="p">);</span>   <span class="c1">// (7)</span>
          <span class="n">panic</span><span class="p">(</span><span class="s">"vmx_launch/resume should not return"</span><span class="p">);</span>
        <span class="k">break</span><span class="p">;</span>
      <span class="p">...</span>
    <span class="p">}</span> <span class="k">while</span> <span class="p">(</span><span class="n">handled</span><span class="p">);</span>
    <span class="n">VMCLEAR</span><span class="p">(</span><span class="n">vmcs</span><span class="p">);</span>
    <span class="k">return</span> <span class="p">(</span><span class="mi">0</span><span class="p">);</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p>(1) is the <code class="language-plaintext highlighter-rouge">struct vm_ops</code> used to store operations for Intel VMX, including <code class="language-plaintext highlighter-rouge">vmx_vminit</code> and <code class="language-plaintext highlighter-rouge">vmx_run</code>. (2) is used to set the VMCS. Note that the host RIP is set as <code class="language-plaintext highlighter-rouge">vmx_longjmp</code>, i.e. on every VMEXIT <code class="language-plaintext highlighter-rouge">vmx_longjmp</code> will be called.</p> <p>For <code class="language-plaintext highlighter-rouge">vmx_run</code>, in (3) the function will set the host CR3 and guest RIP. In (4), <code class="language-plaintext highlighter-rouge">vmm.ko</code> will handle VMEXITs internally, unless userspace emulation is required. In (5), the function will call <code class="language-plaintext highlighter-rouge">vmx_setjmp</code> which is mainly written in assembly to save host registers and other host state. On a VMEXIT, <code class="language-plaintext highlighter-rouge">vmx_longjmp</code> will return to the instruction after <code class="language-plaintext highlighter-rouge">vmx_setjmp</code> (similar to <code class="language-plaintext highlighter-rouge">setjmp</code> and <code class="language-plaintext highlighter-rouge">longjmp</code> in C). Depending on whether VMENTRY occurs for the first time or not, either <code class="language-plaintext highlighter-rouge">vmx_launch</code> (6) or <code class="language-plaintext highlighter-rouge">vmx_resume</code> (7) is called.</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span>
<span class="nf">vmcs_set_defaults</span><span class="p">(</span><span class="k">struct</span> <span class="n">vmcs</span> <span class="o">*</span><span class="n">vmcs</span><span class="p">,</span>
                  <span class="n">u_long</span> <span class="n">host_rip</span><span class="p">,</span> <span class="n">u_long</span> <span class="n">host_rsp</span><span class="p">,</span> <span class="n">u_long</span> <span class="n">ept_pml4</span><span class="p">,</span>
                  <span class="kt">uint32_t</span> <span class="n">pinbased_ctls</span><span class="p">,</span> <span class="kt">uint32_t</span> <span class="n">procbased_ctls</span><span class="p">,</span>
                  <span class="kt">uint32_t</span> <span class="n">procbased_ctls2</span><span class="p">,</span> <span class="kt">uint32_t</span> <span class="n">exit_ctls</span><span class="p">,</span>
                  <span class="kt">uint32_t</span> <span class="n">entry_ctls</span><span class="p">,</span> <span class="n">u_long</span> <span class="n">msr_bitmap</span><span class="p">,</span> <span class="kt">uint16_t</span> <span class="n">vpid</span><span class="p">)</span>
<span class="p">{</span>
  <span class="n">VMPTRLD</span><span class="p">(</span><span class="n">vmcs</span><span class="p">);</span>
  <span class="p">...</span>
  <span class="cm">/* Instruction Pointer */</span>
  <span class="k">if</span> <span class="p">((</span><span class="n">error</span> <span class="o">=</span> <span class="n">vmwrite</span><span class="p">(</span><span class="n">VMCS_HOST_RIP</span><span class="p">,</span> <span class="n">host_rip</span><span class="p">))</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">)</span>
    <span class="k">goto</span> <span class="n">done</span><span class="p">;</span>
  <span class="p">...</span>
  <span class="n">VMCLEAR</span><span class="p">(</span><span class="n">vmcs</span><span class="p">);</span>
  <span class="k">return</span> <span class="p">(</span><span class="n">error</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <p>This function is used to initialize the VMCS. Note that we must call <code class="language-plaintext highlighter-rouge">VMPTRLD</code> to point the CPU to the VMCS before writing fields (we only show setting the host RIP here). We use <code class="language-plaintext highlighter-rouge">VMCLEAR</code> after modifying the fields to flush the VMCS from cache to memory, to ensure that every pCPU has a consistent view of the VMCS in-memory.</p> <pre><code class="language-asm">#define VMX_GUEST_RESTORE \
  movq %rdi,%rsp; \
  movq VMXCTX_GUEST_CR2(%rdi),%rsi; \
  movq %rsi,%cr2; \
  movq VMXCTX_GUEST_RSI(%rdi),%rsi; \
  movq VMXCTX_GUEST_RDX(%rdi),%rdx; \
  movq VMXCTX_GUEST_RCX(%rdi),%rcx; \
  movq VMXCTX_GUEST_R8(%rdi),%r8; \
  movq VMXCTX_GUEST_R9(%rdi),%r9; \
  movq VMXCTX_GUEST_RAX(%rdi),%rax; \
  movq VMXCTX_GUEST_RBX(%rdi),%rbx; \
  movq VMXCTX_GUEST_RBP(%rdi),%rbp; \
  movq VMXCTX_GUEST_R10(%rdi),%r10; \
  movq VMXCTX_GUEST_R11(%rdi),%r11; \
  movq VMXCTX_GUEST_R12(%rdi),%r12; \
  movq VMXCTX_GUEST_R13(%rdi),%r13; \
  movq VMXCTX_GUEST_R14(%rdi),%r14; \
  movq VMXCTX_GUEST_R15(%rdi),%r15; \
  movq VMXCTX_GUEST_RDI(%rdi),%rdi;

#define VM_INSTRUCTION_ERROR(reg) \
  jnc 1f; \
  movl $VM_FAIL_INVALID,reg; /* CF is set */ \
  jmp 3f; \
  1: jnz 2f; \
  movl $VM_FAIL_VALID,reg; /* ZF is set */ \
  jmp 3f; \
  2: movl $VM_SUCCESS,reg; \
  3: movl reg,VMXCTX_LAUNCH_ERROR(%rsp)

ENTRY(vmx_setjmp)
  movq (%rsp),%rax  /* return address */
  movq %r15,VMXCTX_HOST_R15(%rdi)
  movq %r14,VMXCTX_HOST_R14(%rdi)
  movq %r13,VMXCTX_HOST_R13(%rdi)
  movq %r12,VMXCTX_HOST_R12(%rdi)
  movq %rbp,VMXCTX_HOST_RBP(%rdi)
  movq %rsp,VMXCTX_HOST_RSP(%rdi)
  movq %rbx,VMXCTX_HOST_RBX(%rdi)
  movq %rax,VMXCTX_HOST_RIP(%rdi)
  movl $VMX_RETURN_DIRECT,%eax
  ret
END(vmx_setjmp)

ENTRY(vmx_launch) 
  VMX_DISABLE_INTERRUPTS
  VMX_CHECK_AST
  VMX_GUEST_RESTORE 
  vmlaunch

  VM_INSTRUCTION_ERROR(%eax)
  movq %rsp,%rdi
  movq $VMX_RETURN_VMLAUNCH,%rsi
  addq $VMXCTX_TMPSTKTOP,%rsp
  callq vmx_return
END(vmx_launch)
</code></pre> <p>These are some x86 assembly functions for performing saving/restoring of host/guest registers and issuing VT-x instructions. In <code class="language-plaintext highlighter-rouge">vmx_setjmp</code>, we save the host registers into <code class="language-plaintext highlighter-rouge">vmxctx</code> that are not automatically saved by VMCS. In <code class="language-plaintext highlighter-rouge">vmx_launch</code>, we first disable interrupts, restore guest state not automatically restored by VMCS (using a helper macro), then issue <code class="language-plaintext highlighter-rouge">VMLAUNCH</code> instruction. If <code class="language-plaintext highlighter-rouge">VMLAUNCH</code> fails, we record the failure information and call <code class="language-plaintext highlighter-rouge">vmx_return</code>. Otherwise, upon a <code class="language-plaintext highlighter-rouge">VMEXIT</code>, <code class="language-plaintext highlighter-rouge">vmx_longjmp</code> will be called. We show <code class="language-plaintext highlighter-rouge">vmx_return</code> and <code class="language-plaintext highlighter-rouge">vmx_longjmp</code> next.</p> <pre><code class="language-asm">ENTRY(vmx_return)
  movq VMXCTX_HOST_R15(%rdi),%r15
  movq VMXCTX_HOST_R14(%rdi),%r14
  movq VMXCTX_HOST_R13(%rdi),%r13
  movq VMXCTX_HOST_R12(%rdi),%r12
  movq VMXCTX_HOST_RBP(%rdi),%rbp
  movq VMXCTX_HOST_RSP(%rdi),%rsp
  movq VMXCTX_HOST_RBX(%rdi),%rbx
  movq VMXCTX_HOST_RIP(%rdi),%rax 
  movq %rax,(%rsp) /* return address */ 

  movl %esi,%eax
  ret 
END(vmx_return)

/*
* void vmx_longjmp(void)
* %rsp points to the struct vmxctx
*/
ENTRY(vmx_longjmp)
  movq %rdi,VMXCTX_GUEST_RDI(%rsp)
  movq %rsi,VMXCTX_GUEST_RSI(%rsp)
  movq %rdx,VMXCTX_GUEST_RDX(%rsp)
  movq %rcx,VMXCTX_GUEST_RCX(%rsp)
  movq %r8,VMXCTX_GUEST_R8(%rsp)
  movq %r9,VMXCTX_GUEST_R9(%rsp)
  movq %rax,VMXCTX_GUEST_RAX(%rsp)
  movq %rbx,VMXCTX_GUEST_RBX(%rsp)
  movq %rbp,VMXCTX_GUEST_RBP(%rsp)
  movq %r10,VMXCTX_GUEST_R10(%rsp)
  movq %r11,VMXCTX_GUEST_R11(%rsp)
  movq %r12,VMXCTX_GUEST_R12(%rsp)
  movq %r13,VMXCTX_GUEST_R13(%rsp)
  movq %r14,VMXCTX_GUEST_R14(%rsp)
  movq %r15,VMXCTX_GUEST_R15(%rsp)
  movq %cr2,%rdi
  movq %rdi,VMXCTX_GUEST_CR2(%rsp)
  movq %rsp,%rdi
  movq $VMX_RETURN_LONGJMP,%rsi
  addq $VMXCTX_TMPSTKTOP,%rsp
  callq vmx_return
END(vmx_longjmp)
</code></pre> <p><code class="language-plaintext highlighter-rouge">vmx_return</code> restores host state from <code class="language-plaintext highlighter-rouge">vmxctx</code> struct, then returns to the instruction after <code class="language-plaintext highlighter-rouge">vmx_setjmp</code> (observe that <code class="language-plaintext highlighter-rouge">vmx_setjmp</code> saves %RAX into %RSP, which <code class="language-plaintext highlighter-rouge">vmx_return</code> restores, places on the stack, and executes <code class="language-plaintext highlighter-rouge">ret</code>). <code class="language-plaintext highlighter-rouge">vmx_longjmp</code> saves guest registers, then calls <code class="language-plaintext highlighter-rouge">vmx_return</code>, which returns to the instruction after <code class="language-plaintext highlighter-rouge">vmx_setjmp</code>. If <code class="language-plaintext highlighter-rouge">vmx_setjmp</code> returns normally, the return value (held in %RSI) is 0, but if <code class="language-plaintext highlighter-rouge">vmx_setjmp</code> returns through <code class="language-plaintext highlighter-rouge">vmx_longjmp</code>, the return value is 1 (which is the value of <code class="language-plaintext highlighter-rouge">$VMX_RETURN_LONGJMP</code>).</p> <p>We now return to <code class="language-plaintext highlighter-rouge">vmx_run</code>. Previously, we only showed the case when <code class="language-plaintext highlighter-rouge">vmx_setjmp</code> returned normally, that is, <code class="language-plaintext highlighter-rouge">vmm.ko</code> is about to VMENTER. Now, we show the case when <code class="language-plaintext highlighter-rouge">vmx_setjmp</code> returns via <code class="language-plaintext highlighter-rouge">vmx_longjmp</code>, i.e. we return to <code class="language-plaintext highlighter-rouge">vmm.ko</code> via VMEXIT, and now <code class="language-plaintext highlighter-rouge">vmm.ko</code> must perform the necessary handling or return to userspace if necessary.</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">static</span> <span class="kt">int</span>
<span class="nf">vmx_run</span><span class="p">(</span><span class="kt">void</span> <span class="o">*</span><span class="n">arg</span><span class="p">,</span> <span class="kt">int</span> <span class="n">vcpu</span><span class="p">,</span> <span class="n">register_t</span> <span class="n">rip</span><span class="p">)</span>
<span class="p">{</span>
  <span class="p">...</span>
<span class="k">do</span> <span class="p">{</span>
  <span class="n">rc</span> <span class="o">=</span> <span class="n">vmx_setjmp</span><span class="p">(</span><span class="n">vmxctx</span><span class="p">);</span> 
  <span class="k">switch</span> <span class="p">(</span><span class="n">rc</span><span class="p">)</span> <span class="p">{</span>
    <span class="p">...</span>
    <span class="k">case</span> <span class="n">VMX_RETURN_LONGJMP</span><span class="p">:</span> 
      <span class="k">break</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="n">enable_intr</span><span class="p">();</span>  <span class="c1">// (8)</span>

    <span class="cm">/* (9) */</span>
    <span class="n">vmexit</span><span class="o">-&gt;</span><span class="n">rip</span> <span class="o">=</span> <span class="n">rip</span> <span class="o">=</span> <span class="n">vmcs_guest_rip</span><span class="p">();</span> 
    <span class="n">vmexit</span><span class="o">-&gt;</span><span class="n">inst_length</span> <span class="o">=</span> <span class="n">vmexit_instruction_length</span><span class="p">();</span> 
    <span class="n">vmexit</span><span class="o">-&gt;</span><span class="n">u</span><span class="p">.</span><span class="n">vmx</span><span class="p">.</span><span class="n">exit_reason</span> <span class="o">=</span> <span class="n">exit_reason</span> <span class="o">=</span> <span class="n">vmcs_exit_reason</span><span class="p">();</span> 
    <span class="n">vmexit</span><span class="o">-&gt;</span><span class="n">u</span><span class="p">.</span><span class="n">vmx</span><span class="p">.</span><span class="n">exit_qualification</span> <span class="o">=</span> <span class="n">vmcs_exit_qualification</span><span class="p">();</span> 

    <span class="n">handled</span> <span class="o">=</span> <span class="n">vmx_exit_process</span><span class="p">(</span><span class="n">vmx</span><span class="p">,</span> <span class="n">vcpu</span><span class="p">,</span> <span class="n">vmexit</span><span class="p">);</span>  <span class="c1">// (10)</span>
    <span class="n">vmx_exit_trace</span><span class="p">(</span><span class="n">vmx</span><span class="p">,</span> <span class="n">vcpu</span><span class="p">,</span> <span class="n">rip</span><span class="p">,</span> <span class="n">exit_reason</span><span class="p">,</span> <span class="n">handled</span><span class="p">);</span>
  <span class="p">}</span> <span class="k">while</span> <span class="p">(</span><span class="n">handled</span><span class="p">);</span>
  <span class="k">return</span> <span class="p">(</span><span class="mi">0</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <p>In (8), we enable interrupts. In (9), we save information about the VMEXIT. In (10), we call <code class="language-plaintext highlighter-rouge">vmx_exit_process</code>, which sets handled to 1 iff <code class="language-plaintext highlighter-rouge">vmm.ko</code> is able to handle everything internally. Otherwise, we break out of the loop and return back to userspace (i.e. <code class="language-plaintext highlighter-rouge">ioctl</code> returns). Next, we look at the function <code class="language-plaintext highlighter-rouge">vmx_exit_process</code>, specifically the case when the guest VM has performed I/O and device emulation, i.e. <code class="language-plaintext highlighter-rouge">handled = 0</code>:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">static</span> <span class="kt">int</span>
<span class="nf">vmx_exit_process</span><span class="p">(</span><span class="k">struct</span> <span class="n">vmx</span> <span class="o">*</span><span class="n">vmx</span><span class="p">,</span> <span class="kt">int</span> <span class="n">vcpu</span><span class="p">,</span> <span class="k">struct</span> <span class="n">vm_exit</span> <span class="o">*</span><span class="n">vmexit</span><span class="p">)</span>
<span class="p">{</span>
  <span class="kt">int</span> <span class="n">error</span><span class="p">,</span> <span class="n">handled</span><span class="p">;</span>
  <span class="k">struct</span> <span class="n">vmcs</span> <span class="o">*</span><span class="n">vmcs</span><span class="p">;</span>
  <span class="k">struct</span> <span class="n">vmxctx</span> <span class="o">*</span><span class="n">vmxctx</span><span class="p">;</span>
  <span class="kt">uint32_t</span> <span class="n">eax</span><span class="p">,</span> <span class="n">ecx</span><span class="p">,</span> <span class="n">edx</span><span class="p">;</span>
  <span class="kt">uint64_t</span> <span class="n">qual</span><span class="p">,</span> <span class="n">gla</span><span class="p">,</span> <span class="n">gpa</span><span class="p">,</span> <span class="n">cr3</span><span class="p">,</span> <span class="n">intr_info</span><span class="p">;</span>
  <span class="n">vmcs</span> <span class="o">=</span> <span class="o">&amp;</span><span class="n">vmx</span><span class="o">-&gt;</span><span class="n">vmcs</span><span class="p">[</span><span class="n">vcpu</span><span class="p">];</span>
  <span class="n">vmxctx</span> <span class="o">=</span> <span class="o">&amp;</span><span class="n">vmx</span><span class="o">-&gt;</span><span class="n">ctx</span><span class="p">[</span><span class="n">vcpu</span><span class="p">];</span>
  <span class="n">qual</span> <span class="o">=</span> <span class="n">vmexit</span><span class="o">-&gt;</span><span class="n">u</span><span class="p">.</span><span class="n">vmx</span><span class="p">.</span><span class="n">exit_qualification</span><span class="p">;</span>  <span class="c1">// (11)</span>
  <span class="n">vmexit</span><span class="o">-&gt;</span><span class="n">exitcode</span> <span class="o">=</span> <span class="n">VM_EXITCODE_BOGUS</span><span class="p">;</span>
  <span class="k">switch</span> <span class="p">(</span><span class="n">vmexit</span><span class="o">-&gt;</span><span class="n">u</span><span class="p">.</span><span class="n">vmx</span><span class="p">.</span><span class="n">exit_reason</span><span class="p">)</span> <span class="p">{</span>  <span class="c1">// (12)</span>
    <span class="p">...</span>
    <span class="k">case</span> <span class="n">EXIT_REASON_INOUT</span><span class="p">:</span> 
      <span class="cm">/* (13) */</span>
      <span class="n">vmexit</span><span class="o">-&gt;</span><span class="n">exitcode</span> <span class="o">=</span> <span class="n">VM_EXITCODE_INOUT</span><span class="p">;</span> 
      <span class="n">vmexit</span><span class="o">-&gt;</span><span class="n">u</span><span class="p">.</span><span class="n">inout</span><span class="p">.</span><span class="n">bytes</span> <span class="o">=</span> <span class="p">(</span><span class="n">qual</span> <span class="o">&amp;</span> <span class="mh">0x7</span><span class="p">)</span> <span class="o">+</span> <span class="mi">1</span><span class="p">;</span> 
      <span class="n">vmexit</span><span class="o">-&gt;</span><span class="n">u</span><span class="p">.</span><span class="n">inout</span><span class="p">.</span><span class="n">in</span> <span class="o">=</span> <span class="p">(</span><span class="n">qual</span> <span class="o">&amp;</span> <span class="mh">0x8</span><span class="p">)</span> <span class="o">?</span> <span class="mi">1</span> <span class="o">:</span> <span class="mi">0</span><span class="p">;</span> 
      <span class="n">vmexit</span><span class="o">-&gt;</span><span class="n">u</span><span class="p">.</span><span class="n">inout</span><span class="p">.</span><span class="n">string</span> <span class="o">=</span> <span class="p">(</span><span class="n">qual</span> <span class="o">&amp;</span> <span class="mh">0x10</span><span class="p">)</span> <span class="o">?</span> <span class="mi">1</span> <span class="o">:</span> <span class="mi">0</span><span class="p">;</span> 
      <span class="n">vmexit</span><span class="o">-&gt;</span><span class="n">u</span><span class="p">.</span><span class="n">inout</span><span class="p">.</span><span class="n">rep</span> <span class="o">=</span> <span class="p">(</span><span class="n">qual</span> <span class="o">&amp;</span> <span class="mh">0x20</span><span class="p">)</span> <span class="o">?</span> <span class="mi">1</span> <span class="o">:</span> <span class="mi">0</span><span class="p">;</span> 
      <span class="n">vmexit</span><span class="o">-&gt;</span><span class="n">u</span><span class="p">.</span><span class="n">inout</span><span class="p">.</span><span class="n">port</span> <span class="o">=</span> <span class="p">(</span><span class="kt">uint16_t</span><span class="p">)(</span><span class="n">qual</span> <span class="o">&gt;&gt;</span> <span class="mi">16</span><span class="p">);</span> 
      <span class="n">vmexit</span><span class="o">-&gt;</span><span class="n">u</span><span class="p">.</span><span class="n">inout</span><span class="p">.</span><span class="n">eax</span> <span class="o">=</span> <span class="p">(</span><span class="kt">uint32_t</span><span class="p">)(</span><span class="n">vmxctx</span><span class="o">-&gt;</span><span class="n">guest_rax</span><span class="p">);</span> 
      <span class="k">break</span><span class="p">;</span>
    <span class="nl">default:</span>
      <span class="k">break</span><span class="p">;</span>
  <span class="p">}</span>
  <span class="k">return</span> <span class="p">(</span><span class="n">handled</span><span class="p">);</span> 
<span class="p">}</span>
</code></pre></div></div> <p>In (11), we get Exit Qualification information (from VMCS). In (12), we switch on Exit Reason. In (13), we save all necessary information in <code class="language-plaintext highlighter-rouge">struct vm_exit</code>, including length of access (number of bytes), direction of access, whether the access is a string, whether the access contains a rep-prefix (to indicate number of repetitions of the instruction), and port number. Since device emulation in userspace is necessary, this function does not set <code class="language-plaintext highlighter-rouge">handled</code>, i.e. 0 is returned.</p> <h1 id="device-emulation">Device Emulation</h1> <p>In this section, we look at device emulation in userspace. In the above code snippets, <code class="language-plaintext highlighter-rouge">vmm.ko</code> returns from <code class="language-plaintext highlighter-rouge">ioctl</code> to <code class="language-plaintext highlighter-rouge">vm_run</code> in <code class="language-plaintext highlighter-rouge">libvmmapi</code> after a VMEXIT occurs due to the guest VM performing an I/O access. <code class="language-plaintext highlighter-rouge">vm_run</code> then returns to <code class="language-plaintext highlighter-rouge">vm_loop</code>, which invokes the necessary handler based on the exit code. In this case, the handler <code class="language-plaintext highlighter-rouge">vmexit_inout</code> will be invoked, which we show below.</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">static</span> <span class="kt">int</span>
<span class="nf">vmexit_inout</span><span class="p">(</span><span class="k">struct</span> <span class="n">vmctx</span> <span class="o">*</span><span class="n">ctx</span><span class="p">,</span> <span class="k">struct</span> <span class="n">vm_exit</span> <span class="o">*</span><span class="n">vme</span><span class="p">,</span> <span class="kt">int</span> <span class="o">*</span><span class="n">pvcpu</span><span class="p">)</span>
<span class="p">{</span>
  <span class="kt">int</span> <span class="n">error</span><span class="p">;</span>
  <span class="kt">int</span> <span class="n">bytes</span><span class="p">,</span> <span class="n">port</span><span class="p">,</span> <span class="n">in</span><span class="p">,</span> <span class="n">out</span><span class="p">;</span>
  <span class="kt">uint32_t</span> <span class="n">eax</span><span class="p">;</span>
  <span class="kt">int</span> <span class="n">vcpu</span><span class="p">;</span>
  <span class="n">vcpu</span> <span class="o">=</span> <span class="o">*</span><span class="n">pvcpu</span><span class="p">;</span>

  <span class="cm">/* (1) */</span>
  <span class="n">port</span> <span class="o">=</span> <span class="n">vme</span><span class="o">-&gt;</span><span class="n">u</span><span class="p">.</span><span class="n">inout</span><span class="p">.</span><span class="n">port</span><span class="p">;</span> 
  <span class="n">bytes</span> <span class="o">=</span> <span class="n">vme</span><span class="o">-&gt;</span><span class="n">u</span><span class="p">.</span><span class="n">inout</span><span class="p">.</span><span class="n">bytes</span><span class="p">;</span>
  <span class="n">eax</span> <span class="o">=</span> <span class="n">vme</span><span class="o">-&gt;</span><span class="n">u</span><span class="p">.</span><span class="n">inout</span><span class="p">.</span><span class="n">eax</span><span class="p">;</span>
  <span class="n">in</span> <span class="o">=</span> <span class="n">vme</span><span class="o">-&gt;</span><span class="n">u</span><span class="p">.</span><span class="n">inout</span><span class="p">.</span><span class="n">in</span><span class="p">;</span>
  <span class="n">out</span> <span class="o">=</span> <span class="o">!</span><span class="n">in</span><span class="p">;</span>

  <span class="n">error</span> <span class="o">=</span> <span class="n">emulate_inout</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">vcpu</span><span class="p">,</span> <span class="n">in</span><span class="p">,</span> <span class="n">port</span><span class="p">,</span> <span class="n">bytes</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">eax</span><span class="p">,</span> <span class="n">strictio</span><span class="p">);</span>  <span class="c1">// (2)</span>
  <span class="k">if</span> <span class="p">(</span><span class="n">error</span> <span class="o">==</span> <span class="mi">0</span> <span class="o">&amp;&amp;</span> <span class="n">in</span><span class="p">)</span> 
    <span class="n">error</span> <span class="o">=</span> <span class="n">vm_set_register</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">vcpu</span><span class="p">,</span> <span class="n">VM_REG_GUEST_RAX</span><span class="p">,</span> <span class="n">eax</span><span class="p">);</span>  <span class="c1">// (3)</span>
  <span class="k">if</span> <span class="p">(</span><span class="n">error</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span>
    <span class="k">return</span> <span class="p">(</span><span class="n">VMEXIT_CONTINUE</span><span class="p">);</span>  <span class="c1">// (4)</span>
  <span class="k">else</span> <span class="p">{</span>
    <span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"Unhandled %s%c 0x%04x</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span>
    <span class="n">in</span> <span class="o">?</span> <span class="s">"in"</span> <span class="o">:</span> <span class="s">"out"</span><span class="p">,</span>
    <span class="n">bytes</span> <span class="o">==</span> <span class="mi">1</span> <span class="o">?</span> <span class="sc">'b'</span> <span class="o">:</span> <span class="p">(</span><span class="n">bytes</span> <span class="o">==</span> <span class="mi">2</span> <span class="o">?</span> <span class="sc">'w'</span> <span class="o">:</span> <span class="sc">'l'</span><span class="p">),</span> <span class="n">port</span><span class="p">);</span>
    <span class="k">return</span> <span class="p">(</span><span class="n">vmexit_catch_inout</span><span class="p">());</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p>In (1), we get the necessary information for the I/O access. In (2), we call <code class="language-plaintext highlighter-rouge">emulate_inout</code>, which performs I/O emulation. If the guest VM performs an in operation, we return the results by setting the guest %RAX register. In (4), we return <code class="language-plaintext highlighter-rouge">VMEXIT_CONTINUE</code> to <code class="language-plaintext highlighter-rouge">vm_loop</code>, which will resume execution at the next guest instruction. We now look at the function <code class="language-plaintext highlighter-rouge">emulate_inout</code>:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span>
<span class="nf">emulate_inout</span><span class="p">(</span><span class="k">struct</span> <span class="n">vmctx</span> <span class="o">*</span><span class="n">ctx</span><span class="p">,</span> <span class="kt">int</span> <span class="n">vcpu</span><span class="p">,</span> <span class="kt">int</span> <span class="n">in</span><span class="p">,</span> <span class="kt">int</span> <span class="n">port</span><span class="p">,</span> <span class="kt">int</span> <span class="n">bytes</span><span class="p">,</span>
              <span class="kt">uint32_t</span> <span class="o">*</span><span class="n">eax</span><span class="p">,</span> <span class="kt">int</span> <span class="n">strict</span><span class="p">)</span>
<span class="p">{</span>
  <span class="kt">int</span> <span class="n">flags</span><span class="p">;</span>
  <span class="kt">uint32_t</span> <span class="n">mask</span><span class="p">;</span>
  <span class="n">inout_func_t</span> <span class="n">handler</span><span class="p">;</span>
  <span class="kt">void</span> <span class="o">*</span><span class="n">arg</span><span class="p">;</span>
  <span class="n">assert</span><span class="p">(</span><span class="n">port</span> <span class="o">&lt;</span> <span class="n">MAX_IOPORTS</span><span class="p">);</span>
  <span class="n">handler</span> <span class="o">=</span> <span class="n">inout_handlers</span><span class="p">[</span><span class="n">port</span><span class="p">].</span><span class="n">handler</span><span class="p">;</span>  <span class="c1">// (5)</span>
  <span class="k">if</span> <span class="p">(</span><span class="n">strict</span> <span class="o">&amp;&amp;</span> <span class="n">handler</span> <span class="o">==</span> <span class="n">default_inout</span><span class="p">)</span>
    <span class="k">return</span> <span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">);</span>
  <span class="cm">/* (6) */</span>
  <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">in</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">switch</span> <span class="p">(</span><span class="n">bytes</span><span class="p">)</span> <span class="p">{</span>
      <span class="k">case</span> <span class="mi">1</span><span class="p">:</span>
        <span class="n">mask</span> <span class="o">=</span> <span class="mh">0xff</span><span class="p">;</span>
        <span class="k">break</span><span class="p">;</span>
      <span class="k">case</span> <span class="mi">2</span><span class="p">:</span>
        <span class="n">mask</span> <span class="o">=</span> <span class="mh">0xffff</span><span class="p">;</span>
        <span class="k">break</span><span class="p">;</span>
      <span class="nl">default:</span>
        <span class="n">mask</span> <span class="o">=</span> <span class="mh">0xffffffff</span><span class="p">;</span>
        <span class="k">break</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="o">*</span><span class="n">eax</span> <span class="o">=</span> <span class="o">*</span><span class="n">eax</span> <span class="o">&amp;</span> <span class="n">mask</span><span class="p">;</span>  <span class="c1">// (7)</span>
  <span class="p">}</span>
  <span class="n">flags</span> <span class="o">=</span> <span class="n">inout_handlers</span><span class="p">[</span><span class="n">port</span><span class="p">].</span><span class="n">flags</span><span class="p">;</span>
  <span class="n">arg</span> <span class="o">=</span> <span class="n">inout_handlers</span><span class="p">[</span><span class="n">port</span><span class="p">].</span><span class="n">arg</span><span class="p">;</span>
  <span class="k">if</span> <span class="p">((</span><span class="n">in</span> <span class="o">&amp;&amp;</span> <span class="p">(</span><span class="n">flags</span> <span class="o">&amp;</span> <span class="n">IOPORT_F_IN</span><span class="p">))</span> <span class="o">||</span> <span class="p">(</span><span class="o">!</span><span class="n">in</span> <span class="o">&amp;&amp;</span> <span class="p">(</span><span class="n">flags</span> <span class="o">&amp;</span> <span class="n">IOPORT_F_OUT</span><span class="p">)))</span>
    <span class="k">return</span> <span class="p">((</span><span class="o">*</span><span class="n">handler</span><span class="p">)(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">vcpu</span><span class="p">,</span> <span class="n">in</span><span class="p">,</span> <span class="n">port</span><span class="p">,</span> <span class="n">bytes</span><span class="p">,</span> <span class="n">eax</span><span class="p">,</span> <span class="n">arg</span><span class="p">));</span>  <span class="c1">// (8)</span>
  <span class="k">else</span>
    <span class="k">return</span> <span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">);</span>
<span class="p">}</span>

<span class="kt">int</span>
<span class="nf">register_inout</span><span class="p">(</span><span class="k">struct</span> <span class="n">inout_port</span> <span class="o">*</span><span class="n">iop</span><span class="p">)</span>  <span class="c1">// (9)</span>
<span class="p">{</span>
  <span class="n">assert</span><span class="p">(</span><span class="n">iop</span><span class="o">-&gt;</span><span class="n">port</span> <span class="o">&lt;</span> <span class="n">MAX_IOPORTS</span><span class="p">);</span>
  <span class="n">inout_handlers</span><span class="p">[</span><span class="n">iop</span><span class="o">-&gt;</span><span class="n">port</span><span class="p">].</span><span class="n">name</span> <span class="o">=</span> <span class="n">iop</span><span class="o">-&gt;</span><span class="n">name</span><span class="p">;</span>
  <span class="n">inout_handlers</span><span class="p">[</span><span class="n">iop</span><span class="o">-&gt;</span><span class="n">port</span><span class="p">].</span><span class="n">flags</span> <span class="o">=</span> <span class="n">iop</span><span class="o">-&gt;</span><span class="n">flags</span><span class="p">;</span>
  <span class="n">inout_handlers</span><span class="p">[</span><span class="n">iop</span><span class="o">-&gt;</span><span class="n">port</span><span class="p">].</span><span class="n">handler</span> <span class="o">=</span> <span class="n">iop</span><span class="o">-&gt;</span><span class="n">handler</span><span class="p">;</span>
  <span class="n">inout_handlers</span><span class="p">[</span><span class="n">iop</span><span class="o">-&gt;</span><span class="n">port</span><span class="p">].</span><span class="n">arg</span> <span class="o">=</span> <span class="n">iop</span><span class="o">-&gt;</span><span class="n">arg</span><span class="p">;</span>
  <span class="k">return</span> <span class="p">(</span><span class="mi">0</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <p>In (5), we get the registered I/O handler. For guest writes, we set the mask based on length of I/O access in (6) and apply the mask to the value the guest wants to write in (7). In (8), we invoke the handler. We can register an I/O emulation handler using the <code class="language-plaintext highlighter-rouge">register_inout</code> function in (9).</p> <p>We now show examples of device emulation. Our primary focus is on para-virtualized device emulation, which are used due to their high-performance (compared to full-virtualization), but require installing para-virtualized device drivers to the guest OS. Note that more recent developments such as <a href="https://www.dpdk.org/">DPDK</a> and <a href="https://spdk.io/">SPDK</a> bypass the kernel entirely by performing polling in userspace; this prevents each I/O access from going through 2 stacks: the guest VM and the VMM.</p> <h2 id="consport">Consport</h2> <p>We end with a simple example of I/O emulation: emulating a paravirtualized console port. This post is getting too long, so I will continue explaining other examples of device emulation (with the main focus being virtio) in the next post in the series!</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">static</span> <span class="kt">void</span>
<span class="nf">ttywrite</span><span class="p">(</span><span class="kt">unsigned</span> <span class="kt">char</span> <span class="n">wb</span><span class="p">)</span>
<span class="p">{</span>
  <span class="p">(</span><span class="kt">void</span><span class="p">)</span> <span class="n">write</span><span class="p">(</span><span class="n">STDOUT_FILENO</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">wb</span><span class="p">,</span> <span class="mi">1</span><span class="p">);</span>  <span class="c1">// (4) </span>
<span class="p">}</span>

<span class="k">static</span> <span class="kt">int</span>
<span class="nf">console_handler</span><span class="p">(</span><span class="k">struct</span> <span class="n">vmctx</span> <span class="o">*</span><span class="n">ctx</span><span class="p">,</span> <span class="kt">int</span> <span class="n">vcpu</span><span class="p">,</span> <span class="kt">int</span> <span class="n">in</span><span class="p">,</span> <span class="kt">int</span> <span class="n">port</span><span class="p">,</span> <span class="kt">int</span> <span class="n">bytes</span><span class="p">,</span>
                <span class="kt">uint32_t</span> <span class="o">*</span><span class="n">eax</span><span class="p">,</span> <span class="kt">void</span> <span class="o">*</span><span class="n">arg</span><span class="p">)</span>
<span class="p">{</span>
  <span class="k">static</span> <span class="kt">int</span> <span class="n">opened</span><span class="p">;</span>
  <span class="k">if</span> <span class="p">(</span><span class="n">bytes</span> <span class="o">==</span> <span class="mi">2</span> <span class="o">&amp;&amp;</span> <span class="n">in</span><span class="p">)</span> <span class="p">{</span>
    <span class="o">*</span><span class="n">eax</span> <span class="o">=</span> <span class="n">BVM_CONS_SIG</span><span class="p">;</span>
    <span class="k">return</span> <span class="p">(</span><span class="mi">0</span><span class="p">);</span>
  <span class="p">}</span>

  <span class="k">if</span> <span class="p">(</span><span class="n">bytes</span> <span class="o">!=</span> <span class="mi">4</span><span class="p">)</span>
    <span class="k">return</span> <span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">);</span>

  <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">opened</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">ttyopen</span><span class="p">();</span>  <span class="c1">// (1)</span>
    <span class="n">opened</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
  <span class="p">}</span>

  <span class="k">if</span> <span class="p">(</span><span class="n">in</span><span class="p">)</span> 
    <span class="o">*</span><span class="n">eax</span> <span class="o">=</span> <span class="n">ttyread</span><span class="p">();</span>  <span class="c1">// (2)</span>
  <span class="k">else</span>
    <span class="n">ttywrite</span><span class="p">(</span><span class="o">*</span><span class="n">eax</span><span class="p">);</span>  <span class="c1">// (3)</span>
  <span class="k">return</span> <span class="p">(</span><span class="mi">0</span><span class="p">);</span>
<span class="p">}</span>

<span class="k">static</span> <span class="k">struct</span> <span class="n">inout_port</span> <span class="n">consport</span> <span class="o">=</span> <span class="p">{</span>
  <span class="s">"bvmcons"</span><span class="p">,</span>
  <span class="n">BVM_CONSOLE_PORT</span><span class="p">,</span>
  <span class="n">IOPORT_F_INOUT</span><span class="p">,</span>
  <span class="n">console_handler</span>
<span class="p">};</span>

<span class="kt">void</span>
<span class="nf">init_bvmcons</span><span class="p">(</span><span class="kt">void</span><span class="p">)</span>
<span class="p">{</span>
  <span class="n">register_inout</span><span class="p">(</span><span class="o">&amp;</span><span class="n">consport</span><span class="p">);</span>  <span class="c1">// (5)</span>
<span class="p">}</span>
</code></pre></div></div> <p>If the console is not open yet, (1) opens it. If the guest attempts to perform a read, <code class="language-plaintext highlighter-rouge">console_handler</code> calls <code class="language-plaintext highlighter-rouge">ttyread()</code> in (2), otherwise it calls <code class="language-plaintext highlighter-rouge">ttywrite()</code> in (3). <code class="language-plaintext highlighter-rouge">ttywrite()</code> simply performs a write to STDOUT in (4). This device emulator is registered using <code class="language-plaintext highlighter-rouge">register_inout</code> in (5).</p> <h1 id="summary">Summary</h1> <p>In the <a href="">next post</a>, I will continue explaining device emulation in userspace, this time focusing on virtio, which is a common framework for paravirtualized device I/O. Examples of virtio include virtio-net for NICs and virtio-blk for storage devices. I will also dive into the code to understand how <code class="language-plaintext highlighter-rouge">usr/sbin/bhyveload</code> loads and initializes guest VMs into memory.</p> <h1 id="references">References</h1> <p>[1] <a href="https://syuu1228.github.io/howto_implement_hypervisor/">https://syuu1228.github.io/howto_implement_hypervisor/</a> <br/> [2] <a href="https://wiki.freebsd.org/bhyve">https://wiki.freebsd.org/bhyve</a></p>]]></content><author><name></name></author><category term="systems"/><category term="virtualization"/><category term="operating-systems"/><summary type="html"><![CDATA[Taking a close look at FreeBSD's BHyVe.]]></summary></entry><entry><title type="html">Writing a Hypervisor from Scratch - Hardware</title><link href="https://stephentea.github.io/blog/2025/vtx-hardware/" rel="alternate" type="text/html" title="Writing a Hypervisor from Scratch - Hardware"/><published>2025-08-03T00:00:00+00:00</published><updated>2025-08-03T00:00:00+00:00</updated><id>https://stephentea.github.io/blog/2025/vtx-hardware</id><content type="html" xml:base="https://stephentea.github.io/blog/2025/vtx-hardware/"><![CDATA[<p>Virtualization is one of the most important technologies behind modern cloud computing. With increasing hardware support for virtualization provided by CPU vendors such as Intel and AMD over the past 20 years, high performant virtualization is now possible. This article will explain hardware virtualization support from the perspective of someone writing a hypervisor from scratch.</p> <p>Main reference is <a href="https://syuu1228.github.io/howto_implement_hypervisor/">ハイパーバイザの作り方</a>, a Japanese blog series on hypervisor development, but I will supplement these notes with my own experience working on <a href="https://github.com/matsu/bitvisor">BitVisor</a>, a lightweight, para-passthrough hypervisor supporting MacOS, Windows, and Linux, running on top of both Intel and AMD CPUs. Read <a href="https://www.intel.com/content/www/us/en/developer/articles/technical/intel-sdm.html">Intel’s Software Development Manual</a> for an official reference!</p> <h1 id="what-is-virtualization">What is Virtualization?</h1> <p>Virtualization refers to the emulation of computing resources, to allow running multiple isolated environments on a single physical machine. For instance, by using a hypervisor, we can run multiple virtual machines (VMs) on top, each consisting of an operating system along with userspace applications. Another example is network virtualization, which allows running multiple isolated networks on top of a physical network.</p> <div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/vtx-hardware-imgs/vtx-hardware-stack-480.webp 480w,/vtx-hardware-imgs/vtx-hardware-stack-800.webp 800w,/vtx-hardware-imgs/vtx-hardware-stack-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/vtx-hardware-imgs/vtx-hardware-stack.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="cmud" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Non-virtualized computing stack (left), virtualized computing stack, specifically Type 1 VMM (right). </div> <p>This article will focus on the former, i.e. computing virtualization (for network virtualization, refer to my other blog post on Software Defined Networking <a href="">here</a>). Within this, we will focus specifically on Intel VT (Vanderpool Technology), although similar technologies exist for AMD, Arm, and other CPU vendors, hence concepts covered in this article are easily translatable.</p> <h1 id="intel-vt-x">Intel VT-x</h1> <p>A non-virtualized software stack involves running an OS in kernel-space and applications in user-space. With traditional virtualization, we run the hypervisor (also called VMM) in kernel-space, and run multiple OS + applications in user-space. When the guest OS executes a sensitive instruction in user-space, for example, setting CR3 or performing device I/O with <code class="language-plaintext highlighter-rouge">INB</code> and <code class="language-plaintext highlighter-rouge">OUTB</code>, we expect the CPU to <strong>trap</strong> these instructions, transferring control to the hypervisor, which allows the hypervisor to emulate these instructions.</p> <p>Historically, x86 has been hard to virtualize, as many sensitive instructions such as <code class="language-plaintext highlighter-rouge">POPF</code> do not trap when executed in userspace. <a href="https://www.vmware.com/">VMWare’s</a> solution to this was to use binary translation, i.e. statically analyze guest OS binary to replace sensitive instructions with instructions that trap. This requires no modifications to the guest OS source. On the other hand, <a href="https://xenproject.org/">Xen’s</a> solution was to use <strong>paravirtualization</strong>, i.e. modifying guest OS source code to user <strong>hypercalls</strong> that rely on the existence of a hypervisor. This requires modifications to the guest OS source, but can achieve higher performance than full emulation.</p> <p>In 2003, Intel introduced VT-x, which is a set of extensions to their CPUs to better support virtualization. In addition to the 4 rings of protection (kernel-mode at ring 0 and user-mode at ring 3), VT-x introduces <strong>root mode</strong> and <strong>non-root mode</strong>. We run hypervisors in the former, and guest VMs in the latter. Transitioning from root mode to non-root mode is called VMENTRY, while transitioning from non-root mode to root mode is called VMEXIT.</p> <div class="row"> <div class="col-sm mt-3 mt-md-0 text-center"> <figure> <picture> <source class="responsive-img-srcset" srcset="/vtx-hardware-imgs/vtx-hardware-rootnonroot-480.webp 480w,/vtx-hardware-imgs/vtx-hardware-rootnonroot-800.webp 800w,/vtx-hardware-imgs/vtx-hardware-rootnonroot-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/vtx-hardware-imgs/vtx-hardware-rootnonroot.png" class="img-fluid rounded z-depth-1" width="400" height="300" title="root-non-root" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Root mode (hypervisor) on left, non-root mode (guest VM) on right. Transition from root mode to non-root mode is called VMENTRY, transition from non-root mode to root mode is called VMEXIT. </div> <p>Let’s first take a look at the lifecycle of a VM. First, the hypervisor must enable VT-x by setting the VMXE bit in CR4 to 1 (must be done in ring 0). Next, the hypervisor must allocate a <strong>Virtual Machine Control Structure (VMCS)</strong> (page-aligned in memory) to specify what will trigger VMEXIT from non-root mode to root mode (we will discuss VMCS in more detail in the next section). Note that VMCS is an opaque data structure to hypervisors, hence we must read and write to VMCS using dedicated instructions.</p> <p>After initializing a VMCS for each vCPU (virtual CPU) of the guest VM, the hypervisor will use <code class="language-plaintext highlighter-rouge">VMPTRLD</code> to point each vCPU to the corresponding VMCS in memory, then use <code class="language-plaintext highlighter-rouge">VMCLEAR</code> to flush VMCS from cache to memory. Before launching the guest VM, we must save hypervisor state by writing certain registers to the VMCS (these will be restored by the CPU on the next VMEXIT) and other registers using <code class="language-plaintext highlighter-rouge">PUSHA</code>, <code class="language-plaintext highlighter-rouge">PUSHF</code>.</p> <p>After saving hypervisor state, we will restore guest state by restoring registers using <code class="language-plaintext highlighter-rouge">POPA</code>, <code class="language-plaintext highlighter-rouge">POPF</code>. Finally, we can enter non-root mode using <code class="language-plaintext highlighter-rouge">VMLAUNCH</code> (if first time entering guest VM) or <code class="language-plaintext highlighter-rouge">VMRESUME</code> (for all later times). Certain guest state saved in the VMCS will be restored by the CPU automatically. Now, the guest VM wil run until VMEXIT occurs (for example, guest OS executes a sensitive instruction or an external interrupt arrives).</p> <p>When VMEXIT occurs, the CPU will write the reason for exit in the VMCS, automatically save certain guest registers, restore certain host registers, and switch to root mode. The hypervisor must save other guest registers not saved by the CPU (for instance, by using <code class="language-plaintext highlighter-rouge">MOV</code>) and restore other host registers not done by the CPU (for instance, using <code class="language-plaintext highlighter-rouge">POPA</code>, <code class="language-plaintext highlighter-rouge">POPF</code>).</p> <p>Now, the hypervisor will investigate the reason for VMEXIT and perform the proper emulation, for example, emulating device I/O or returning system state information to the guest. Before resuming the guest with <code class="language-plaintext highlighter-rouge">VMRESUME</code>, the hypervisor must issue <code class="language-plaintext highlighter-rouge">VMCLEAR</code> to flush VMCS from cache to memory (since another vCPU may run next). This cycle continues for the lifetime of the guest VM.</p> <div class="row"> <div class="col-sm mt-3 mt-md-0 text-center"> <figure> <picture> <source class="responsive-img-srcset" srcset="/vtx-hardware-imgs/vtx-hardware-lifecycle-480.webp 480w,/vtx-hardware-imgs/vtx-hardware-lifecycle-800.webp 800w,/vtx-hardware-imgs/vtx-hardware-lifecycle-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/vtx-hardware-imgs/vtx-hardware-lifecycle.png" class="img-fluid rounded z-depth-1" width="400" height="300" title="root-non-root" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Lifecycle of a VM. Hypervisor launches a guest VM by configuring VMCS, then uses VMLAUNCH. Guest VM transfers control back to hypervisor upon VMEXIT. Hypervisor performs emulation, then starts guest VM using VMRESUME. </div> <h1 id="virtual-machine-control-structure-vmcs">Virtual Machine Control Structure (VMCS)</h1> <p>VMCS is an in-memory structure used to configure guest VMs. Hypervisors must read <code class="language-plaintext highlighter-rouge">VMREAD</code>, <code class="language-plaintext highlighter-rouge">VMWRITE</code> to read/write from VMCS. To initialize a VMCS, hypervisors must use <code class="language-plaintext highlighter-rouge">VMPTRLD</code> to point the vCPU to the VCMS, use <code class="language-plaintext highlighter-rouge">VMCLEAR</code> to clear all fields, and use <code class="language-plaintext highlighter-rouge">VMWRITE</code> to set the fields. VT-x also provides <code class="language-plaintext highlighter-rouge">VMCALL</code> and <code class="language-plaintext highlighter-rouge">VMFUNC</code> to allow guest VMs to make hypercalls for paravirtualization. VMCS contains the following fields:</p> <div class="row"> <div class="col-sm mt-3 mt-md-0 text-center"> <figure> <picture> <source class="responsive-img-srcset" srcset="/vtx-hardware-imgs/vtx-hardware-vmcs-480.webp 480w,/vtx-hardware-imgs/vtx-hardware-vmcs-800.webp 800w,/vtx-hardware-imgs/vtx-hardware-vmcs-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/vtx-hardware-imgs/vtx-hardware-vmcs.png" class="img-fluid rounded z-depth-1" width="400" height="300" title="vmcs" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Fields within Virtual Machine Control Structure (VMCS). </div> <ul> <li> <p><strong>VMCS Revision Identifier</strong> - Contains the revision number of the VMCS. As VT-x has been changed over the years, this allows different CPUs to properly read the VMCS when migrating a VMCS from one physical machine to another.</p> </li> <li> <p><strong>VMCS Abort Indicator</strong> - If an error occurs during VMEXIT, the CPU writes down error information in this field. If VMEXIT occurs successfully, this field will not be used.</p> </li> <li> <p><strong>Guest State Area</strong> - The CPU automatically saves and restores guest information here. Registers include: CR0, CR3, CR4, DR7, RSP, RIP, RFLAGS, CS, SS, DS, ES, FS, GS, LDTR, TR, GDTR, SMBASE, plus MSR registers including: IA32_DEBUGCTL, IA32_SYSENTER_CS, IA32_SYSENTER_ESP, IA32_SYSENTER_EIP, IA32_PERF_GLOBAL_CTRL, IA32_PAT, and IA32_EFER.</p> </li> </ul> <ul> In addition, the CPU saves the state of each vCPU, information about each segment, pending/ blocked interrupts, VMX pre-emption timer counter, EPT PTE address, and more. The hypervisor must save and restore other information not included here (for instance, general purpose registers such as RAX, RBX, RCX). </ul> <ul> <li> <p><strong>Host State Area</strong> - The CPU automatically saves and restores host information here. Registers include: CR0, CR3, CR4, RSP, RIP, CS, SS, DS, ES, FS, GS, LDTR, TR, GDTR, plus MSR registers including: IA32_SYSENTER_CS, IA32_SYSENTER_ESP, IA32_SYSENTER_EIP, IA32_PERF_GLOBAL_CTRL, IA32_PAT, and IA32_EFER. The hypervisor must save and restore other information not included here (for instance, general purpose registers such as RAX, RBX, RCX).</p> </li> <li> <p><strong>VM Execution Control Fields</strong> - This field configures CPU behavior when executing guest VMs, including a bitmap of on/off flags for whether to VMEXIT on various reasons, on/off flag for EPT (we will cover this in the next section), EPT address, settings for virtual local APIC (we will cover this in <a href="#interrupt-virtualization">Interrupt Virtualization</a>), VPID, etc.</p> </li> <li> <p><strong>VM Exit Control Fields</strong> - This field configures CPU behavior during VMEXIT, for instance, whether to save/restore MSR, on/off flag for hypervisor 64-bit mode, etc.</p> </li> <li> <p><strong>VM Entry Control Fields</strong> - This field configures CPU behavior during VMENTRY, for instance, configuring delivery of virtual interrupts, whether to save/restore MSR, on/off flag for guest 64-bit mode, etc.</p> </li> <li> <p><strong>VM Exit Information Fields</strong> - When a VMEXIT occurs, the CPU writes exit information here. Reasons include: exception, NMI, external interrupt, internal interrupt, triple fault, INIT signal, SIPI signal, SMI signal, task switch, <code class="language-plaintext highlighter-rouge">CPUID</code> instruction, SMX-related instructions, <code class="language-plaintext highlighter-rouge">HLT</code> instruction, cache-related instructions (<code class="language-plaintext highlighter-rouge">INVD</code>, <code class="language-plaintext highlighter-rouge">WBINVD</code>), TLB-related instructions (<code class="language-plaintext highlighter-rouge">HNVLPG</code>, <code class="language-plaintext highlighter-rouge">INVPCID</code>), I/O-related instructions (<code class="language-plaintext highlighter-rouge">INB</code>, <code class="language-plaintext highlighter-rouge">OUTB</code>), performance monitoring counter-related instructions (<code class="language-plaintext highlighter-rouge">RDPMC</code>), timestamp counter-related instructions (<code class="language-plaintext highlighter-rouge">RDTSC</code>), SMM-related instructions, <code class="language-plaintext highlighter-rouge">MONITOR</code>/<code class="language-plaintext highlighter-rouge">MWAIT</code> instructions, <code class="language-plaintext highlighter-rouge">PAUSE</code> instruction, <code class="language-plaintext highlighter-rouge">RDRAND</code> instruction, VT-x instructions, access to control registers / debug registers / MSR / APIC / GDTR / IDTR / LDTR / TR, and expiry of VMX pre-emption timer.</p> </li> </ul> <h1 id="memory-virtualization">Memory Virtualization</h1> <p><strong>Virtual memory</strong> provides each process with an isolated, uniform address space. OS configures page tables (2-level for 32-bit, 4-level for 64-bit), which the MMU hardware uses to map virtual address issued by the CPU to the corresponding physical address. In addition, page tables provide access information, such as read (R) / write (W) / execute (X) permissions, and accessed (A) and dirty (D) flags to allow the OS to make decisions about paging in and out of disk.</p> <div class="row"> <div class="col-sm mt-3 mt-md-0 text-center"> <figure> <picture> <source class="responsive-img-srcset" srcset="/vtx-hardware-imgs/vtx-hardware-virtual-memory-480.webp 480w,/vtx-hardware-imgs/vtx-hardware-virtual-memory-800.webp 800w,/vtx-hardware-imgs/vtx-hardware-virtual-memory-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/vtx-hardware-imgs/vtx-hardware-virtual-memory.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="virtual-memory" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Paging structures for translating virtual address (VA) to physical address (PA). </div> <p>When running guest OSes on top of a hypervisor, we must virtualize virtual memory, since guest OSes configure page tables to map from guest virtual addresses (GVA) to guest physical addresses (GPA), which are likely different from the underlying host physical addresses (HPA). There are two main techniques for doing this: shadowing paging and EPT.</p> <p><strong>Shadow paging</strong> is a virtualization technique done exclusively in software. Whenever the guest modifies CR3 or changes a page table entry, the hypervisor creates a copy of paging structures (called shadow paging structures) by walking guest structures, translating every GPA to the corresponding HPA, then sets CR3 to point to the shadow PML4 table.</p> <div class="row"> <div class="col-sm mt-3 mt-md-0 text-center"> <figure> <picture> <source class="responsive-img-srcset" srcset="/vtx-hardware-imgs/vtx-hardware-shadow-paging-480.webp 480w,/vtx-hardware-imgs/vtx-hardware-shadow-paging-800.webp 800w,/vtx-hardware-imgs/vtx-hardware-shadow-paging-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/vtx-hardware-imgs/vtx-hardware-shadow-paging.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="shadow-paging" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> The paging structures configured by the guest VM (lower) maps GVA to GPA, but GPA does not necessariy direct-map to HPA. Therefore, shadow paging structures configured by the hypervisor (upper) are used to correctly map GVA to HPA. </div> <p><strong>Extended Page Table (EPT)</strong> refers to hardware support by CPUs in which the hypervisor creates a 4-level page table that translates from GPA to HPA. By enabling EPT and setting the Extended Page Table Pointer (EPTP) to point to the PML4 table, the hardware will perform nested translation for every GVA. That is, instead of accessing the GPA of the PML4 table pointed to by CR3, hardware will translate the GPA of the PML4 table into HPA, then access this table, and so on. Using EPT removes the overhead of VMEXIT on every write to CR3 or changes to page table entries found in shadow paging (hypervisors usually write-protect guest paging structures in this case).</p> <div class="row"> <div class="col-sm mt-3 mt-md-0 text-center"> <figure> <picture> <source class="responsive-img-srcset" srcset="/vtx-hardware-imgs/vtx-hardware-ept-480.webp 480w,/vtx-hardware-imgs/vtx-hardware-ept-800.webp 800w,/vtx-hardware-imgs/vtx-hardware-ept-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/vtx-hardware-imgs/vtx-hardware-ept.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="ept" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> The hypervisor configures the EPT (extended page table) to map from GPA to HPA, which the MMU uses to translate every GPA it encounters while walking guest paging structures. </div> <p>Additionally, VT-x introduces <strong>Virtual Process Identifier (VPID)</strong> which can be configured in the VMCS for each guest VM to tag the TLB. Previously, software had to flush the entire TLB on every VMEXIT / VMENTRY to prevent wrong translations from being used from one guest VM to another. Using VPID allows hardware to differentiate between translation entries from different guest VMs, which prevents the need to flush the entire TLB, hence improving performance. This is similar to PASID used in IOTLBs.</p> <h1 id="io-virtualization">I/O Virtualization</h1> <p>One of the main role of an OS is to abstract access to devices using common interfaces, for instance, Unix files or Berkeley web sockets. <strong>Device drivers</strong> within OSes are responsible for interaction with hardware, for instance, writing to configuration registers to perform initial setup, reading/ writing to data and control registers, and setting up DMA for large transfers of data. Device drivers perform I/O in two ways: <strong>I/O mapped I/O</strong> (also called port-mapped I/O) and <strong>memory-mapped I/O (MMIO)</strong>.</p> <p>For I/O mapped I/O, software reads/ writes from an I/O address space, independent of the existing address space for main memory. For instance, we can use <code class="language-plaintext highlighter-rouge">INB</code> and <code class="language-plaintext highlighter-rouge">OUTB</code> to read/ write from specified ports. The hypervisor can configure all accesses to I/O ports to VMEXIT by setting Unconditional I/O Exiting to 1 in VM Execution Control Fields, or set Use I/O Bitmaps to 1 and set the bits corresponding to the I/O addresses we wish to VMEXIT on.</p> <div class="row"> <div class="col-sm mt-3 mt-md-0 text-center"> <figure> <picture> <source class="responsive-img-srcset" srcset="/vtx-hardware-imgs/vtx-hardware-io-mapped-io-480.webp 480w,/vtx-hardware-imgs/vtx-hardware-io-mapped-io-800.webp 800w,/vtx-hardware-imgs/vtx-hardware-io-mapped-io-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/vtx-hardware-imgs/vtx-hardware-io-mapped-io.png" class="img-fluid rounded z-depth-1" width="400" height="300" title="io-mapped-io" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> VMCS contains pointers to I/O Bitmap A and I/O Bitmap B. The former corresponds to 0x0000 ~ 0x7FFFF of the I/O address space, while the latter corresponds to 0x8000 ~ 0xFFFF. </div> <p>To emulate I/O behavior, we must know the access direction (read or write), the access size (number of bytes), and the port number. These information is found in the VM Exit Information Fields in the VMCS. In addition, for I/O mapped I/O, non-string accesses must use the RAX register, while string accesses must use the memory region pointed to by ES:ESI. This allows the hypervisor to fully emulate I/O behavior.</p> <p>For instance, if the guest attempts to read a non-string from port 80, the hypervisor can return return fake information by writing to RAX. Similarly, if the guest attempts to write a string to port 80, the hypervisor can read the string from address specified by ES:ESI, then perform the corresponding write (either to an emulated device or physical device).</p> <p>For memory-mapped I/O, the hypervisor can set the corresponding page table entries to not-present (P = 0) for shadow paging, or set both the read and write access bits to 0 for EPT. This will cause either a PF-exception or EPT-violation when the guest performs MMIO, transferring control to the hypervisor. However, VT-x does not provide further information about the access direction, access size, or the source/destination of data.</p> <p>Therefore, the hypervisor must perform instruction emulation. Specifically, the hypervisor will first obtain the RIP from the VMCS, read the instruction that caused the VMEXIT from guest memory, decode this instruction to obtain the necessary information, then use this to perform device emulation. This is a extra overhead compared to I/O mapped I/O, hence VT-x provides optimized alternatives for commonly accessed hardware registers such as APIC and TPR (more on this in the next section).</p> <h1 id="interrupt-virtualization">Interrupt Virtualization</h1> <p>Interrupts are used for asynchronous coordination between CPU and external devices. There are 2 types of interrupts: <strong>external</strong> and <strong>internal</strong>.</p> <ul> <li> <p>Within external interrupts, there are <strong>maskable</strong> and <strong>non-maskable interrupts (NMI)</strong>.<br/> The former are used by standard devices, such as keystrokes and timer interrupts. The latter are usually used for non-recoverable hardware errors. External interrupts are asynchronous to the instruction stream.</p> </li> <li> <p>Within internal interrupts, there are <strong>exceptions</strong> and <strong>software interrupts</strong>. The formers are used to notify the OS of errors during execution (for example, page faults, divide by 0, etc). The latter are used for userspace applications to trap into the kernel, for instance using <code class="language-plaintext highlighter-rouge">INT</code> for system calls in x86-32. Internal interrupts are synchronous to the instruction stream (hence cannot be masked).</p> </li> </ul> <p>Traditionally, CPUs were connected to a PIC (Programmable Interrupt Controller) - sometimes daisy-chained - that directs interrupts from hardware devices to the CPU. Modern architectures use <strong>APICs</strong> (Advanced Programmable Interrupt Controller).</p> <div class="row"> <div class="col-sm mt-3 mt-md-0 text-center"> <figure> <picture> <source class="responsive-img-srcset" srcset="/vtx-hardware-imgs/vtx-hardware-interrupts-480.webp 480w,/vtx-hardware-imgs/vtx-hardware-interrupts-800.webp 800w,/vtx-hardware-imgs/vtx-hardware-interrupts-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/vtx-hardware-imgs/vtx-hardware-interrupts.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="io-mapped-io" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Architecture of interrupt delivery in modern systems. Each CPU has a local APIC, which can use IPIs to deliver interrupts to other CPUs. I/O APIC is connected to legacy PCI devices, with newer PCIe devices using MSI/MSI-X. </div> <h2 id="understanding-interrupt-hardware">Understanding Interrupt Hardware</h2> <p>Each CPU has a <strong>local APIC</strong>, which is responsible for allocating vector numbers to interrupts, delivering interrupts to the CPU by invoking the correct handler in the <strong>IDT</strong> (Interrupt Descriptor Table), and receiving EOI (End of Interrupt) from CPUs. In addition, local APIC has extra features such as timers, temperature sensors, performance monitoring counters, and delivering/ receiving <strong>IPIs</strong> (inter-processor interrupts, used for TLB shootdown, for example).</p> <p>Local APIC contains the following registers:</p> <ul> <li> <p><strong>Interrupt Request Register (IRR)</strong> - Bitmap of interrupts that have not been delivered to the CPU yet (read-only).</p> </li> <li> <p><strong>In-Service Register (ISR)</strong> - Contains the interrupt to be delivered next. After the CPU writes to EOI, the local APIC copies the highest priority interrupt from the IRR to the ISR (if pending interrupts exist), then delivers the interrupt in ISR by looking up the IDT and invoking the corresponding handler.</p> </li> <li> <p><strong>End of Interrupt (EOI)</strong> - Used by CPU to notify local APIC that it is done handling the interrupt (write-only). Local APIC will then clear the ISR, and if IRR is non-zero, it will deliver the next highest-priority interrupt.</p> </li> <li> <p><strong>Processor Priority Register (PPR)</strong> - Used by local APIC to decide whether to mask interrupts or not. Bits [7:4] is the priority class, while bits [3:0] is the priority sub-class (only the upper bits are used for interrupt masking). If the PPR has a higher priority class than the highest priortiy interrupt in IRR, the interrupt will be masked - otherwise, it will be delivered.</p> </li> <li> <p><strong>Task Priority Register (TPR)</strong> - Used by local APIC to update PPR. Similar to PPR, TPR consists of priority class (bits [7:4]) and priority sub-class (bits [3:0]). If TPR[7:4] is greater or equal to PPR[7:4], PPR will be set to TPR. Otherwise, PPR[7:4] will be set to ISR[7:4] and PPR[3:0] will be cleared to 0.</p> </li> <li> <p><strong>Local APIC ID Register (LAPIC)</strong> - Used to identify local APIC in the system. For instance, I/O APIC uses LAPIC to redirect external interrupts to specific local APIC(s). IPI uses LAPIC to specify which CPU to deliver interrupts to and identify which CPU interrupts come from.</p> </li> <li> <p><strong>Interrupt Command Register (ICR)</strong> - Used to deliver IPIs.</p> </li> <li> <p><strong>Logical Destination Register (LDR)</strong> - Used to specify which LAPIC to deliver IPI to.</p> </li> <li> <p><strong>Destination Format Register (DFR)</strong> - Local APICs can be organized in different ways, for instance, flat model (using flat addressing) or cluster model (using hierarchical addressing). This register specifies which organizational model to use.</p> </li> </ul> <p>I/O APIC is connected to up to 24 external devices and is responsible for routing external interrupts to local APIC(s) using the <strong>Redirection Table</strong>. A redirection table entry consists of Destination Mode, Delivery Mode, Destination, and Vector fields. Vector field specifies the external interrupt to redirect.</p> <p>If Destination Mode is Physical, Destination specifies the LAPIC to deliver interrupts to. If Destination Mode is Logical, Destination is a bitmask of which LAPICs to deliver interrupts to (for instance, <code class="language-plaintext highlighter-rouge">0000_0011b</code> delivers interrupts to both <code class="language-plaintext highlighter-rouge">0000_0001b</code> and <code class="language-plaintext highlighter-rouge">0000_0010b</code>).</p> <p>If Destination Mode is Logical, we can further specify Delivery Mode. If Delivery Mode is Fixed, the I/O APIC will delivery interrupts to every LAPIC specified in Destination. If Delivery Mode is Lowest Priority, the I/O APIC will deliver the interrupt to the LAPIC with lowest TPR. If there are multiple local APICs with the same TPR, interrupts will be delivered in a round-robin manner.</p> <p>In the original specification, the Destination field was only 8-bits, i.e. the I/O APIC can only support up to 8 CPUs. From Nehalem onwards, Intel has introduced x2APIC (2nd-generation APIC) which expands the Destination field to 32-bits, i.e. the I/O APIC can now support up to 32 CPUs.</p> <p>Using I/O APIC alone has two problems: (1) there are only 24 external pins, hence multiple hardware devices may be forced to share the same pin, and (2) one hardware device can only deliver one type of interrupt. To fix this, <strong>MSI</strong> (Message Signalled Interrupt) and <strong>MSI-X</strong> (Extended Message Signalled Interrupt) are used. MSI delivers interrupts directly to local APICs via PCIe, instead of going through the I/O APIC.</p> <p>MSI supports up to 32 different vector numbers for a single device, while MSI-X extends this to 2048. To configure where interrupts are delivered, the OS writes to the PCIe device configuration registers using entries similar to the redirection table for I/O APIC.</p> <h2 id="virtualizing-interrupt-hardware">Virtualizing Interrupt Hardware</h2> <p>Hypervisors usually do not virtualize internal interrupts or IDT. Internal interrupts occur within non-root mode and are handled directly by looking up the IDT (which is guest-configured), without causing VMEXIT. Alternatively, hypervisors can configure the VMCS to cause a VMEXIT on internal interrupts, for instance for debugging purposes (counting the number of internal interrupts that occur in guest mode).</p> <p>On ther other hand, hypervisors usually configure the VMCS to cause a VMEXIT upon receiving an external interrupt. The hypervisor must then deliver to the correct vCPU by reading the redirection table in the emulated I/O APIC, updating the registers in the emulated local APIC, and configuring delivery of virtual interrupts in the VMCS, then performing a VMENTER (the CPU will automatically deliver virtual interrupts according to information in VMCS).</p> <p>The hypervisor must emulate the local APIC to ensure the guest OS sees a consistent state between receiving interrupts and local APIC registers (that is, delivering virtual interrupts alone is not enough). However, guest OSes access local APIC registers via MMIO, which has an additional overhead of instruction emulation, as discussed in <a href="#io-virtualization">I/O Virtualization</a>.</p> <p>As accesses to local APIC registers are time-critical, VT-x allows hypervisors to set Virtualize APIC Accesses flag to 1 and set the corresponding APIC Access Page address in the VMCS. Then, when VMEXIT occurs due to MMIO access to local APIC registers, access information will be provided in Exit Information fields, without having to perform instruction emulation. Similarly, VT-x allows hypervisors to set TPR Shadow flag to 1 in VMCS to allow efficient TPR shadowing (i.e. VMEXIT iff TPR value is set below a threshold).</p> <p>To emulate delivery of virtual interrupts, the hypervisor must take the following steps:</p> <ol> <li>Set the corresponding bit in IRR.</li> <li>Wait until the vCPU VMEXIT (this will happen for external interrupts, but may not happen immediately for interrupts from emulated devices) or force VMEXIT using IPI.</li> <li>Clear the highest-priority bit in the and copy this to ISR.</li> <li>Update PPR using TSR and ISR.</li> <li>Configure VM Entry Information Field to deliver virtual interrupt.</li> <li>Execute VMRESUME.</li> </ol> <p>To emulate receipt of EOI from vCPUs, the hypervisor must take the following steps:</p> <ol> <li>If IRR is 0, execute VMRESUME. Otherwise, clear the highest priority bit in IRR and copy this to ISR.</li> <li>Update PPR using TSR and ISR.</li> <li>Configure VM Entry Information Field to deliver virtual interrupt.</li> <li>Execute VMRESUME.</li> </ol> <p>The hypervisor must emulate the I/O APIC as well. As I/O APIC are only accessed by guests upon startup, VT-x does not provide optimized shadowing features (unlike local APIC). The hypervisor will keep a redirection table mapping from interrupt vector numbers to vCPUs.</p> <p>The hypervisor emulates MSI/MSI-X in a similar way to I/O APIC. The hypervisor will trap MMIO accesses to the configuration registers of PCIe device, and maintain a table (similar to the redirection table for I/O APIC) to map interrupt vector numbers to vCPUs.</p> <h1 id="summary">Summary</h1> <p>With this, we have covered enough of modern hardware virtualization support technologies to start diving into implementing a hypervisor using software. In the <a href="">next post</a> in this blog series, I will explain the details and dive into the code behind <a href="https://bhyve.org/">BHyVe</a>, an open-source hypervisor for FreeBSD (similar to KVM/QEMU for Linux). For those wanting to learn more about passthrough virtualization and Intel VT-d, read my blog post <a href="">here</a> (I have research experience in specifically this area)!</p> <h1 id="references">References</h1> <p>[1] <a href="https://syuu1228.github.io/howto_implement_hypervisor/">https://syuu1228.github.io/howto_implement_hypervisor/</a> <br/> [2] <a href="https://www.intel.com/content/www/us/en/developer/articles/technical/intel-sdm.html">https://www.intel.com/content/www/us/en/developer/articles/technical/intel-sdm.html</a></p>]]></content><author><name></name></author><category term="systems"/><category term="virtualization"/><category term="operating-systems"/><summary type="html"><![CDATA[Taking a close look at hardware virtualization support.]]></summary></entry></feed>